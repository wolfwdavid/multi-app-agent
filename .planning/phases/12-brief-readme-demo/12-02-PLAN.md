---
phase: 12-brief-readme-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DEMO.md
  - SUBMISSION.md
autonomous: false
requirements: [SHIP-03]

must_haves:
  truths:
    - "DEMO.md is a precise 2:00 shooting script. It has 6 timestamped beats with the same boundaries as README (0:00, 0:12, 0:35, 1:00, 1:15, 1:45, 2:00), the exact URL to open, exact clicks as `[btn: Label]` (each label exists in web/src), and word-budgeted narration of at most 290 words total"
    - "The script includes a caught-silent-failure beat (Evals page step-through: the tool returned OK, read-back found nothing, the run was not reported ok), a rerun beat (every action `deduped`, 0 new writes) and an eval dashboard beat (pass rate, pass^k over N, taxonomy)"
    - "DEMO.md has a pre-flight checklist: Ollama warmed with qwen3.5:4b, a fresh hero sprint run (output kept out of the repo), browser zoom and window size, secrets/.env/notifications closed, the surface chosen by HTTP check (Vercel live if public, else Pages replay, else local static server), and a timed dry run"
    - "Every number spoken in the narration comes from evals.json, hero-run.json or this session's smoke-real output"
    - "The user records and uploads the video (human-only checkpoint) and supplies: the video link, the submission destination (or 'unknown'), team info, the Vercel public/private choice, and push approval"
    - "SUBMISSION.md lists the repo, Pages, HF Space and Vercel URLs (or 'private: Deployment Protection on'), the BRIEF link, the video link, team info and the destination, with no [[USER: placeholders left after the checkpoint"
    - "The push happens only after user approval and after the 12-01 commit exists. Trailers are stripped (tree hash unchanged), an independent grep of outgoing messages is clean, and the outgoing diff has no secrets. The Pages deploy run for HEAD then succeeds and the public URLs return 200"
  artifacts:
    - path: "DEMO.md"
      provides: "2-minute shooting script, pre-flight checklist, fallbacks, recording and upload steps, video link"
      contains: "Caught silent failure"
    - path: "SUBMISSION.md"
      provides: "Copy-paste submission fields and final checklist"
      contains: "https://github.com/wolfwdavid/multi-app-agent"
  key_links:
    - from: "DEMO.md [btn: ...] labels"
      to: "web/src/routes/+page.svelte, web/src/routes/evals/+page.svelte and components"
      via: "literal label strings present in source"
      pattern: "\\[btn: "
    - from: "SUBMISSION.md"
      to: "BRIEF.md / README.md / DEMO.md on GitHub"
      via: "https://github.com/wolfwdavid/multi-app-agent/blob/main/BRIEF.md"
      pattern: "blob/main/BRIEF.md"
    - from: "git push origin main"
      to: ".github/workflows/deploy-pages.yml run for HEAD"
      via: "gh run watch --exit-status + live URL checks"
      pattern: "gh run watch"
---

<objective>
Produce the demo and the submission, then publish:
1. DEMO.md: an exact 2-minute script plus pre-flight checklist, built from verified numbers and real UI labels. SUBMISSION.md: copy-paste fields and a final checklist.
2. A blocking human-action checkpoint. The user rehearses, records and uploads the video and provides the fields only they know. The agent can't record video, and the submission form/URL is unknown.
3. After approval: fill in the fields, commit by explicit path, strip trailers, push, confirm the Pages deploy for HEAD succeeds (plus the HF redeploy), and check that every public URL returns 200.

Purpose: SHIP-03 (the demo script with a caught failure and an idempotent re-run, recorded) and ROADMAP Phase 12 criteria 3-4 (demo recorded; showcase redeployed with final numbers; submission before 7:00 PM ET).
Timing (ET): Task 1 6:15-6:25 (parallel with 12-01) → user records 6:25-6:40 → Task 3 push 6:40-6:45 → user submits by 6:45-6:50. The build window closes at 7:00 PM.
Parallelism: this plan is wave 1 alongside 12-01 because DEMO.md and SUBMISSION.md share no files with it. Task 3's push is hard-gated on the `docs(12-01)` commit and 12-01-SUMMARY.md, so the brief and README ship in the same push.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@tasks/lessons.md
@scripts/strip-ai-trailers.sh
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md

<conventions>
- SHELL: Git Bash. `R="/c/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon"`; `V2="${TMPDIR:-/tmp}/tp-demo"`. Quote every path.
- OWNERSHIP: create or edit only `DEMO.md` and `SUBMISSION.md`. Do NOT edit README.md or BRIEF.md (owned by 12-01), or anything under `web/`, `scripts/` or `.github/`. Never run `npm run record` or `scripts/eval.ts` with default outputs; always pass `--out`/`--json` into `$V2` so committed recordings stay intact.
- NUMBERS: every number in DEMO.md/SUBMISSION.md comes from `web/static/data/evals.json`, `web/static/data/hero-run.json`, `web/static/data/silent-failure-run.json`, this session's smoke output, or `.planning/phases/12-brief-readme-demo/12-01-FACTS.txt` when it exists. Never from memory.
- USER FIELDS: use the visible placeholder syntax `[[USER: description]]`. Angle brackets would hide as HTML when rendered. After the checkpoint, none may remain. A field the user declares unknown gets explicit text (see Task 3).
- DOCS: no mention of AI coding assistants. Do not show the demo student's name/email/GPA. Never print secret values.
- GIT (tasks/lessons.md):
  - Commit by explicit path only: `git add DEMO.md SUBMISSION.md && git commit --no-verify --only -m "docs(12-02): demo script and submission checklist" -- DEMO.md SUBMISSION.md`.
  - The message must have no Co-Authored-By/Claude/Claude-Session/AI trailer.
  - Before pushing:
    - `bash scripts/strip-ai-trailers.sh origin/main`
    - the independent check `git log origin/main..HEAD --format=%B | grep -inE "co-authored|claude|anthropic"` must print nothing
    - tree hash unchanged across the strip
    - no other agent committing (no `.git/index.lock`, `git diff --cached --name-only` empty)
  - Never force-push. Never stash, reset or checkout other agents' uncommitted work.
- DEPLOYMENT PROTECTION on Vercel is the USER's decision. Never change it; only report the HTTP status.

Canonical timeline (identical to README from 12-01):
| 0:00–0:12 | Problem hook |
| 0:12–0:35 | Sprint page: demo profile → gap report (verify links, feasibility warnings) |
| 0:35–1:00 | Plan cards → Approve all → trace → verified artifacts (drafts only) |
| 1:00–1:15 | Run again → every action deduped, 0 new writes |
| 1:15–1:45 | Evals: pass rate + pass^k over N, taxonomy, caught silent failure step-through |
| 1:45–2:00 | Real apps (live counts) + links |

UI copy to expect (07-UI-SPEC; confirm each label in source before using it):
- Nav: "Sprint", "Evals"
- CTAs: "Build plan", "Approve all ({n})", "Run approved actions", "Run again", "Play", "Step", "Next step", "Restart", "Verify on official page"
- Headings: "Caught silent failure"
- Replay banner starts "Recorded run."
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automation (URL/surface check, Ollama warm, fresh scratch recording, smoke counts, UI label check) and draft DEMO.md + SUBMISSION.md</name>
  <files>DEMO.md, SUBMISSION.md</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md sections "Screen 1: Sprint page", "Screen 2: Eval dashboard" (silent failure replay), "Screen 3: Static showcase behavior", and the copy table
    - web/src/routes/+page.svelte, web/src/routes/evals/+page.svelte, and the components they import under web/src/lib (actual button labels and headings)
    - web/static/data/evals.json, web/static/data/hero-run.json, web/static/data/silent-failure-run.json
    - web/src/lib/ui/config.ts (LIVE_URL) and .planning/phases/09-live-backend-on-vercel-rest-api/09-02-SUMMARY.md if present (Vercel URL, protection outcome)
    - .planning/phases/12-brief-readme-demo/12-01-FACTS.txt, if 12-01 has produced it (reuse its SMOKE_/URL facts; otherwise capture them here)
    - web/scripts/sprint.ts header usage (the `--out` and `--json` flags)
  </read_first>
  <action>
1. Automated pre-flight. Run each step and write results to `$V2/preflight.txt` (`mkdir -p "$V2"`).
   a. Snapshot `git -C "$R" status --porcelain -- web/static > "$V2/static-before.txt"`.
   b. URL codes:
      - Pages `/multi-app-agent/`, `/multi-app-agent/evals/` and `/multi-app-agent/data/evals.json`
      - HF Space `https://huggingface.co/spaces/WolfDavid/multi-app-agent`
      - Repo `https://github.com/wolfwdavid/multi-app-agent`
      - Vercel: `VERCEL_URL` from the non-empty LIVE_URL, else 09-02-SUMMARY's production alias, else none. Then `$VERCEL_URL/api/health` with no bypass header.
      - Use `curl -s -o /dev/null -w "%{http_code}"` for all of them.
   c. SURFACE decision, recorded as `SURFACE=`:
      - `vercel-live` if Vercel health is 200 and the body JSON has `target: "vercel"`
      - else `pages-replay` if Pages `/` and `/evals/` are 200
      - else `local-static`: `node scripts/smoke-static.mjs --base multi-app-agent --serve --port 4174` → `http://127.0.0.1:4174/multi-app-agent/`. Only verify it would work (`test -f scripts/smoke-static.mjs` and `web/build/index.html` exists); don't leave a server running.
   d. Warm Ollama. This isn't needed for the recorded UI path; it covers the optional terminal beat and judge Q&A:
      `curl -s -m 120 http://127.0.0.1:11434/api/generate -d '{"model":"qwen3.5:4b","prompt":"ready","stream":false,"think":false,"keep_alive":"45m"}' | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{console.log(JSON.parse(s).done?'OLLAMA_WARM':'OLLAMA_NOT_READY')}catch{console.log('OLLAMA_NOT_READY')}})"`
      Record the result. If it isn't ready, DEMO.md tells the user to run `ollama run qwen3.5:4b "ready" --think=false` or skip the LLM mention.
   e. Fresh hero sprint to scratch (same as `npm run record`, but it doesn't overwrite the committed recording):
      `cd "$R/web" && npx tsx scripts/sprint.ts --profile demo --auto-approve --rerun --out "$V2/demo-sprint.jsonl" --json "$V2/hero-run.json" > "$V2/record.out" 2>&1`
      HARD RULE: never run sprint.ts without BOTH `--out` and `--json` pointing into `$V2`. With `--profile demo --auto-approve` and no `--json`, sprint.ts falls back to writing the tracked, committed `web/static/data/hero-run.json` (`defaultHero`). Without `--out`, it writes the tracked `web/static/traces/demo-sprint.jsonl`. Before running, confirm both flags are parsed: `grep -cE "case '--(out|json)'" "$R/web/scripts/sprint.ts"` must print 2. If it prints less than 2, or the CLI exits with `unknown flag`, STOP: do not run sprint.ts with any flag dropped, record `RECORD_RERUN=not run (sprint.ts --json/--out unsupported)`, and report `sprint.ts --json/--out unsupported` to the orchestrator. (sprint.ts supports both flags today.)
      Record `RECORD_RERUN=<the "RERUN: ..." line>` and `RECORD_STATUS=<status line>`.
      Then diff `git status --porcelain -- web/static` against `static-before.txt`. If the run touched committed files, restore ONLY those two files with `git -C "$R" checkout -- web/static/data/hero-run.json web/static/traces/demo-sprint.jsonl`, and only if they were clean in the snapshot.
   f. Smoke counts: reuse `SMOKE_*` from 12-01-FACTS.txt if present. Otherwise run `cd "$R/web" && npx tsx scripts/smoke-real.ts`, sanitized with the same sed as 12-01 Task 1 step 4, keeping `[tag] app detail` lines only.
   g. Numbers via node:
      - n, the scripted model label, `totals.fake` passRate/runs, passK share, scenario count, adversarial count
      - the LLM model name and N if present
      - hero `plan.actions.length`, `rerun.report.counts.deduped`, plan action counts by app/tool
      - silent-failure `scenarioId`, `report.status`, `before.report.status`, `oracle.failureClass`
   h. Label check: for each label you intend to use, `grep -rlF "<label>" "$R/web/src"`. Use only labels that are found. If a UI-SPEC label differs in code, use the code's label.

2. Write `DEMO.md` (repo root) with these sections, in order:
   - `# TransferPilot: 2-minute demo`, then `## Video` with `Watch: [[USER: video link (YouTube unlisted / Loom)]]`.
   - `## Surface and tabs`: the chosen SURFACE and its URL. Tab 1: sprint page. Tab 2: `/evals/`. Tab 3: `https://github.com/wolfwdavid/multi-app-agent/blob/main/BRIEF.md`. One terminal (Git Bash) sized to about 100x28, font 16+, cwd `web/`. For `pages-replay`, one honest sentence: the site replays a recorded mock-mode run and the banner says so; narration never calls it live.
   - `## Pre-flight (T-15 min)`: a checkbox list with exact commands and settings:
     1. Notifications off (Windows Focus Assist / Do Not Disturb). Close email, chat and password-manager windows.
     2. Close every editor tab showing `web/.env`. Never run `env`, `printenv` or `cat .env` on camera. Clear the terminal (`clear`) and hide shell history.
     3. Browser: a fresh profile or guest window, 1920x1080, zoom 125% (Ctrl + / Ctrl -), bookmarks bar hidden, extensions hidden. Pre-load all three tabs once and check the replay banner or live mode appears.
     4. `ollama ps` shows `qwen3.5:4b` loaded. Otherwise run the warm command from step 1d (keep_alive 45m).
     5. Fresh sprint run to scratch: the exact command from step 1e, with both `--out` and `--json` kept (dropping `--json` overwrites the committed hero-run.json), confirming the `RERUN: 0 new writes (<deduped>/<actions> deduped)` line. Leave that output scrolled to the RERUN line in the terminal for the optional beat.
     6. `npx tsx scripts/smoke-real.ts` once, so the 1:45 beat shows live counts. If the network is flaky, keep this pre-flight output on screen and say "checked a few minutes ago".
     7. Recorder: OBS (Display Capture, 1920x1080, 30 fps, mic level test) or Windows Snipping Tool video recording (Win+Shift+R). Record one full dry run with a timer and aim for 1:50-1:55.
   - `## Script (2:00)`: a markdown table with columns `| Time | Screen | Do | Say |` and EXACTLY the 6 canonical rows. Every click is written `[btn: <Label>]`. Word budgets for the Say cell: ≤ 30, 55, 60, 35, 70, 35 words respectively (≤ 285 total). Content:
     - 0:00–0:12: Screen = sprint tab. Say: the scattered transfer tools and the GAO credit-loss stat from BRIEF §1. TransferPilot turns a profile plus target schools into checked results inside the student's own apps, gated by approval.
     - 0:12–0:35: Do = scroll the profile panel (demo profile is preloaded), scroll to the gap report, hover one `[btn: Verify on official page]`, point at the GPA-below-minimum warning and the no-transfer-program warning. Say: the deterministic, sourced gap analysis; no LLM decides these numbers.
     - 0:35–1:00: Do = `[btn: Build plan]` (live) or scroll to the plan (replay), then `[btn: Approve all]` and `[btn: Run approved actions]`, and watch the timeline end in the verified artifacts panel. Say: <HERO_ACTIONS> actions across Notion, Calendar, Docs and Gmail (use the real per-app counts); signed plan and per-action approval; Gmail drafts only, no send method exists; each artifact is marked verified by reading the app back.
     - 1:00–1:15: Do = `[btn: Run again]`. Say: same plan, same idempotency keys; every action comes back `deduped`, 0 new writes (<deduped>/<actions>). If the button is absent, flash the terminal RERUN line instead.
     - 1:15–1:45: Do = Evals nav, pause on stat cards and the per-scenario table (pass^k column), then scroll to `Caught silent failure` and press `[btn: Play]` or `[btn: Next step]` to the read-back step. Say: <scenario count> scenarios (<adversarial> adversarial) × N=<n> on fresh seeded mock worlds; the independent oracle reads final state, not the agent's report; <totals pass rate> overall, with pass^k shown. Silent failure: the Calendar API said OK but saved nothing; read-back found nothing, so the run reported <report.status>, not ok; with the verifier off it would have said <before.report.status>, and that is a <failureClass>. Mention the LLM column (<model>, N=<n>) only if present.
     - 1:45–2:00: Do = terminal smoke-real lines (or the BRIEF connected-apps table), then the BRIEF tab. Say: live GitHub (<count>) and Hugging Face (<count>) evidence, plus the Notion status from smoke; everything is in the repo with the reliability brief. Close with the product name.
   - `## Fallbacks`, one line each:
     - Pages down → local-static command
     - Run again hidden → terminal RERUN line
     - smoke-real offline → the pre-flight output
     - Vercel protected → use Pages replay and never show a login wall
     - over time → cut the 0:12 beat to 15 s by skipping the hover
   - `## Recording and upload`:
     - trim to ≤ 2:00
     - export MP4 1080p
     - upload to YouTube as Unlisted (or Loom), wait for processing, open the link in a private window to confirm it plays
     - paste the link into the checkpoint reply
   - `## Why these beats`: 3 bullets mapping the beats to the judging criteria (reliability & evaluation 25%, technical execution 30%, demo clarity 10%).

3. Write `SUBMISSION.md` (repo root):
   - `# Submission: TransferPilot`, and a deadline line: `Build window closes 7:00 PM ET, 2026-09-13; target submitted by 6:45 PM ET.`
   - A `## Fields` table (Field | Value):
     - Project name: TransferPilot
     - One-liner (≤ 25 words, from the README first paragraph)
     - Repository: https://github.com/wolfwdavid/multi-app-agent (HTTP <code>)
     - Live showcase (GitHub Pages): https://wolfwdavid.github.io/multi-app-agent/ (HTTP <code>)
     - Eval dashboard: https://wolfwdavid.github.io/multi-app-agent/evals/
     - HF Space: https://huggingface.co/spaces/WolfDavid/multi-app-agent (HTTP <code>)
     - Live backend (Vercel): the URL if health is 200, else `<url>: private (Vercel Deployment Protection on)`, or `not deployed`. Also `[[USER: keep private or make public? (public|private)]]`
     - System & reliability brief: https://github.com/wolfwdavid/multi-app-agent/blob/main/BRIEF.md
     - README: https://github.com/wolfwdavid/multi-app-agent#readme
     - Demo script: https://github.com/wolfwdavid/multi-app-agent/blob/main/DEMO.md
     - Demo video: [[USER: video link]]
     - Team: [[USER: team name, members, emails/handles]]
     - Submission destination: [[USER: form URL / platform, or "unknown"]]
     - External apps used: Notion, Google Calendar, Google Docs/Drive, Gmail (drafts), GitHub, Hugging Face (+ Obsidian local), each with its live/credential-gated status from the smoke facts
   - `## Copy-paste: short description` (≤ 60 words).
   - `## Copy-paste: how we know it works` (≤ 100 words), from the evals numbers, the silent failure and the rerun dedupe.
   - `## Final checklist`, checkboxes:
     - docs(12-01) and docs(12-02) committed
     - trailers stripped, independent grep clean
     - pushed
     - Pages deploy run green for HEAD
     - Pages, /evals/ and HF return 200
     - deployed evals.json commitSha matches the committed one
     - video link opens in a private window
     - form submitted at ___ ET (user)
     - confirmation email/screenshot saved (user)
   - A note at the end: `The submission form URL was not published by organizers at planning time (registration form closed). Check https://multiappagenthackathon.com/ and organizer email/Discord.`

4. Run the verify command. Do NOT commit yet; Task 3 commits after the user fills the fields.
  </action>
  <verify>
    <automated>cd "/c/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && node -e "
const fs=require('fs');const p=require('path');
const d=fs.readFileSync('DEMO.md','utf8');const s=fs.readFileSync('SUBMISSION.md','utf8');const bad=[];
for(const t of ['0:00','0:12','0:35','1:00','1:15','1:45','2:00'])if(!d.includes(t))bad.push('time:'+t);
for(const k of ['deduped','pass^k','qwen3.5:4b','--auto-approve --rerun','--json','--out','zoom','.env','Caught silent failure','smoke-real'])if(!d.toLowerCase().includes(k.toLowerCase()))bad.push('missing:'+k);
const walk=dir=>fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(p.join(dir,e.name)):/\.(svelte|ts)$/.test(e.name)?[p.join(dir,e.name)]:[]);
const src=walk('web/src').map(f=>fs.readFileSync(f,'utf8')).join('\n');
const labels=[...new Set([...d.matchAll(/\[btn: ([^\]]+)\]/g)].map(m=>m[1].replace(/\s*\(.*\)\s*$/,'').trim()))];
if(labels.length<4)bad.push('few-labels:'+labels.length);
for(const l of labels)if(!src.includes(l))bad.push('label-not-in-src:'+l);
const rows=d.split(/\r?\n/).filter(l=>/^\| *[0-2]:[0-5][0-9]/.test(l));
if(rows.length!==6)bad.push('script-rows:'+rows.length);
const words=rows.map(r=>{const c=r.split('|').map(x=>x.trim()).filter(Boolean);return c[c.length-1]}).join(' ').split(/\s+/).filter(Boolean).length;
if(words>290)bad.push('too-many-words:'+words);
if(/\{\{|TODO\(/.test(d+s))bad.push('placeholders');
if(/claude|anthropic|chatgpt|gemini|copilot|generated with/i.test(d+s))bad.push('ai-mention');
if(/Alex Rivera|alex\.rivera@example\.com/i.test(d+s))bad.push('pii');
if(/(\bntn_[A-Za-z0-9]{20,}|\bsecret_[A-Za-z0-9]{30,}|ya29\.[A-Za-z0-9_-]{20,}|\bghp_[A-Za-z0-9]{30,}|\bgsk_[A-Za-z0-9]{20,}|\bsk-[A-Za-z0-9_-]{20,})/.test(d+s))bad.push('secret-shaped');
for(const u of ['https://github.com/wolfwdavid/multi-app-agent','https://wolfwdavid.github.io/multi-app-agent/','https://huggingface.co/spaces/WolfDavid/multi-app-agent','blob/main/BRIEF.md','blob/main/DEMO.md'])if(!s.includes(u))bad.push('sub:'+u);
if((s.match(/\[\[USER:/g)||[]).length<3||!d.includes('[[USER:'))bad.push('user-fields');
const hero=JSON.parse(fs.readFileSync('web/static/data/hero-run.json','utf8'));if(!d.includes(String(hero.plan.actions.length)))bad.push('hero-count-not-used');
const ev=JSON.parse(fs.readFileSync('web/static/data/evals.json','utf8'));if(!d.includes('N='+ev.n)&&!d.includes('N = '+ev.n))bad.push('eval-n-not-used');
if(bad.length){console.error('BAD',bad);process.exit(1)}console.log('DEMO_DRAFT_OK',labels.length,'labels',words,'words')" && test -z "$(git status --porcelain -- web/static | diff - "${TMPDIR:-/tmp}/tp-demo/static-before.txt")" && echo STATIC_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `DEMO_DRAFT_OK` and `STATIC_UNTOUCHED`
    - DEMO.md has exactly 6 script rows at the canonical boundaries, ≥ 4 `[btn: ...]` labels each found in `web/src`, and ≤ 290 narration words
    - DEMO.md mentions `deduped`, `Caught silent failure`, `pass^k`, `qwen3.5:4b`, the `--auto-approve --rerun` command with both `--out` and `--json` into scratch, zoom, `.env` and smoke-real, and uses the hero action count and eval N from the JSON files
    - `$V2/preflight.txt` records SURFACE, all URL codes, OLLAMA_WARM or OLLAMA_NOT_READY, RECORD_RERUN, and the smoke counts
    - SUBMISSION.md contains the repo, Pages, HF, BRIEF and DEMO links plus ≥ 3 `[[USER:` fields; neither doc contains secret-shaped strings, PII or AI-assistant mentions
    - `git status --porcelain -- web/static` is identical to the pre-flight snapshot, and nothing is committed yet
  </acceptance_criteria>
  <done>The user has a precise, rehearsable 2-minute script on a verified surface with real numbers and real button labels, a checklist whose automated steps already ran, and a submission sheet waiting only on the fields only they know.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User rehearses, records and uploads the 2-minute demo and supplies the submission fields plus push approval</name>
  <files>none (human action; no file changes by the agent)</files>
  <action>Show the user the chosen SURFACE and URL, the pre-flight results (Ollama, RECORD_RERUN, smoke counts, URL codes), and the path to DEMO.md. Print `git log origin/main..HEAD --oneline` so the user sees what a push would publish. If the `docs(12-01)` commit isn't there yet, say that the push will wait for it. Then present the steps below and wait. Do not commit, push or deploy before the resume signal.</action>
  <verify>
    <automated>echo "HUMAN CHECKPOINT: resume only on the user's reply containing video=, destination=, team=, vercel=, push="</automated>
  </verify>
  <done>The user has replied with all five fields.</done>
  <what-built>
    DEMO.md (6-beat, 2:00 script with exact clicks, narration and fallbacks) and SUBMISSION.md (fields and checklist), both drafted from verified numbers. Automated pre-flight is done: surface URL checked, Ollama qwen3.5:4b warmed, fresh hero sprint run to scratch showing the RERUN dedupe line, live smoke counts captured. Recording a video is human-only, and so are the submission destination and team details.
  </what-built>
  <how-to-verify>
    1. Open DEMO.md and finish the manual pre-flight items: notifications off, `.env` closed, browser zoom 125% at 1920x1080, the three tabs pre-loaded, the terminal at the RERUN line.
    2. Do one timed dry run. Target 1:50-1:55. If you run over, use the "over time" fallback.
    3. Record (OBS or Win+Shift+R), trim to ≤ 2:00, and export an MP4.
    4. Upload as YouTube Unlisted (or Loom). Open the link in a private window and confirm it plays.
    5. Decide on Vercel: keep Deployment Protection on (the submission lists it as private) or turn it off yourself in the Vercel dashboard. The agent never changes it.
    6. Find the submission destination (organizer site/email/Discord), or answer "unknown".
    7. Approve publishing. The push sends every unpushed commit to the public repo, which redeploys Pages, and `bash scripts/deploy-hf.sh` overwrites the public HF Space with the final build.
  </how-to-verify>
  <resume-signal>Reply with five lines: `video=<url>` · `destination=<url, platform, or unknown>` · `team=<name, members, handles>` · `vercel=public|private` · `push=yes|no`. Or describe issues with the script to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fill user fields, commit by path, gate on 12-01, strip trailers, push, confirm Pages run + HF redeploy + public URLs 200</name>
  <files>DEMO.md, SUBMISSION.md</files>
  <read_first>
    - The user's resume reply (video, destination, team, vercel, push)
    - DEMO.md, SUBMISSION.md (Task 1)
    - scripts/strip-ai-trailers.sh and tasks/lessons.md (trailer stripping, tree-hash check, explicit-path commits)
    - .planning/phases/12-brief-readme-demo/12-01-SUMMARY.md (must exist before pushing)
    - .github/workflows/deploy-pages.yml, scripts/smoke-static.mjs and scripts/deploy-hf.sh, if present
  </read_first>
  <action>
1. Fill the fields:
   - DEMO.md `## Video`: the video URL.
   - SUBMISSION.md: video, team and destination.
   - If destination is `unknown`, write `Not published by organizers as of <time> ET: check https://multiappagenthackathon.com/, organizer email and Discord; submit repo + video + BRIEF links there`.
   - Vercel row:
     - If the user said `public`, re-check `curl -s -o /dev/null -w "%{http_code}" "$VERCEL_URL/api/health"`. If it's 200, write the URL. If it's still 302/401, keep `private (Vercel Deployment Protection on)` and tell the user it isn't public yet.
     - If the user said `private`, write `<url>: private (Vercel Deployment Protection on)`.
   - Check the video link: `curl -s -L -o /dev/null -w "%{http_code}" "<video>"`. Record the code; this is informational, not blocking.
   - Afterwards `grep -c "\[\[USER:" DEMO.md SUBMISSION.md` must total 0.

2. Commit, from the repo root:
   - Run `git diff --cached --name-only` and note what's there.
   - `git add DEMO.md SUBMISSION.md && git commit --no-verify --only -m "docs(12-02): demo script and submission checklist" -- DEMO.md SUBMISSION.md`
   - `git log -1 --format=%B | grep -ciE "co-authored|claude|anthropic|session"` must print 0.

3. If `push=no`: stop here. Report that the commits are local and give the user the exact push sequence from step 5.

4. PUSH GATES. All must pass. If one fails, STOP and report which, without partial pushes:
   - `git log --oneline -40 --grep="docs(12-01)"` is non-empty AND `.planning/phases/12-brief-readme-demo/12-01-SUMMARY.md` exists. If not, report "waiting for 12-01" to the orchestrator and do not push.
   - `test ! -f .git/index.lock` and `git diff --cached --name-only` is empty (no other agent mid-commit).
   - Outgoing secrets (test files excluded, since `*.test.ts` connector fixtures hold intentional fake tokens, same as 12-01's scan): `git log origin/main..HEAD -p --no-color -- . ':(exclude)*.test.ts' ':(exclude)**/*.test.ts' | grep -cE '(\bntn_[A-Za-z0-9]{20,}|\bsecret_[A-Za-z0-9]{30,}|ya29\.[A-Za-z0-9_-]{20,}|\b1//0[A-Za-z0-9_-]{20,}|\bghp_[A-Za-z0-9]{30,}|\bgsk_[A-Za-z0-9]{20,}|\bsk-[A-Za-z0-9_-]{20,})'` prints 0.
   - `git ls-files | grep -E '(^|/)\.env(\.[^/]*)?$' | grep -vc '\.env\.example$'` prints 0.

5. Strip trailers and push:
   - `TREE_BEFORE=$(git rev-parse HEAD^{tree})`, then `bash scripts/strip-ai-trailers.sh origin/main`.
     - It prints `clean: ...` and exits 0 with nothing to strip, even with other agents' unstaged files.
     - If it refuses with "working tree has uncommitted changes" (trailers exist AND the tree is dirty), STOP. Report the dirty paths (`git status --porcelain`) to the orchestrator and user. Never stash or reset other agents' work.
   - `test "$(git rev-parse HEAD^{tree})" = "$TREE_BEFORE"`
   - `git log origin/main..HEAD --format=%B | grep -inE "co-authored|claude|anthropic"` prints nothing.
   - `git push origin main`. On rejection (non-fast-forward), STOP and report. Never force-push.

6. Confirm the deploys:
   - Pages:
     - `H=$(git rev-parse HEAD)`
     - `gh run list --workflow deploy-pages.yml --branch main --limit 5 --json databaseId,headSha,status` → pick the entry with `headSha == H`. Re-list after a few seconds if it isn't registered yet.
     - `gh run watch <id> --exit-status`
     - On failure: `gh run view <id> --log-failed | tail -n 40`. Report it to the user immediately. The previous Pages deploy stays live, so the submission links still work. Do not change code unless the user asks.
   - Live static smoke:
     - `node scripts/smoke-static.mjs --url https://wolfwdavid.github.io/multi-app-agent --wait 300` if the script exists (expect `SMOKE OK`)
     - else curl for 200 on `/multi-app-agent/`, `/multi-app-agent/evals/` and `/multi-app-agent/data/evals.json`
     - Then check that the deployed `data/evals.json` `commitSha` equals the committed file's.
   - HF (push=yes approves it): if `scripts/deploy-hf.sh` exists, `bash scripts/deploy-hf.sh`. A not-logged-in error is an auth gate: ask the user to run `hf auth login` and retry once. Then `curl` the Space URL and expect 200.
   - Final URL table, recorded in the SUMMARY and shown to the user; don't commit it (that would need another push):
     - repo, blob/main/BRIEF.md, blob/main/DEMO.md, blob/main/SUBMISSION.md
     - Pages `/` and `/evals/`, HF Space
     - Vercel health
     - video link
     - each with its HTTP code

7. Tell the user to submit NOW. Give them the destination and the SUBMISSION.md link, and remind them to tick "form submitted at ___ ET" themselves.
  </action>
  <verify>
    <automated>cd "/c/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && test "$(grep -c '\[\[USER:' DEMO.md SUBMISSION.md | awk -F: '{s+=$2} END {print s}')" = "0" && C=$(git log -1 --format=%H --grep="docs(12-02)") && test -n "$C" && test "$(git show --name-only --format= "$C" | sort | tr '\n' ' ')" = "DEMO.md SUBMISSION.md " && test "$(git log -1 --format=%B "$C" | grep -ciE 'co-authored|claude|anthropic|session')" = "0" && test -z "$(git log origin/main..HEAD --format=%B | grep -inE 'co-authored|claude|anthropic')" && git fetch -q origin main && git merge-base --is-ancestor "$C" origin/main && test "$(curl -s -o /dev/null -w '%{http_code}' https://wolfwdavid.github.io/multi-app-agent/)" = "200" && test "$(curl -s -o /dev/null -w '%{http_code}' https://github.com/wolfwdavid/multi-app-agent/blob/main/SUBMISSION.md)" = "200" && echo PUBLISHED_OK</automated>
  </verify>
  <acceptance_criteria>
    - With push=yes, the verify command prints `PUBLISHED_OK`: no `[[USER:` left, the `docs(12-02)` commit touches only DEMO.md and SUBMISSION.md with a clean message, it is an ancestor of origin/main, no outgoing AI trailers, and Pages and SUBMISSION.md on GitHub return 200
    - With push=no: the commit exists locally, nothing was pushed, and the user got the exact push sequence
    - The `docs(12-01)` commit was on origin/main before or in the same push, which the 12-01 gate enforces
    - The HEAD tree hash is equal before and after `strip-ai-trailers.sh`; no force push was used
    - `gh run watch` for the HEAD run exited 0, or its failure was reported to the user with the log tail and the prior deploy confirmed still live (Pages 200)
    - The deployed `data/evals.json` commitSha matches the committed one, and HF redeploy is ok (Space 200) or reported as an auth gate
    - Vercel Deployment Protection settings were never touched
  </acceptance_criteria>
  <done>Final docs and demo are public on GitHub, the Pages showcase is redeployed with the final numbers, the HF Space is updated, all public URLs are confirmed, and the user has everything needed to submit before 7:00 PM ET.</done>
</task>

</tasks>

<verification>
1. DEMO_DRAFT_OK: 6 canonical rows, labels exist in source, ≤ 290 words, caught silent failure plus deduped rerun plus eval dashboard beats, pre-flight with Ollama/record/zoom/.env.
2. The human checkpoint was completed with video, destination, team, vercel and push fields.
3. No `[[USER:` placeholders remain; both commits (12-01, 12-02) are made by explicit path with clean messages.
4. Before push: 12-01 gate, no index lock or staged foreign files, outgoing secret grep 0, trailers stripped with an unchanged tree hash, independent grep clean.
5. After push: Pages run for HEAD green, live static smoke OK, deployed evals.json SHA matches, HF Space 200, GitHub blob URLs 200.
</verification>

<success_criteria>
- ROADMAP Phase 12 criterion 3 (SHIP-03): the 2-minute demo script shows the sprint, a caught silent failure and an idempotent re-run, and the demo is recorded (video link in DEMO.md and SUBMISSION.md).
- ROADMAP Phase 12 criterion 4: the static showcase is redeployed with the final numbers, the secret scan of outgoing commits is clean, and the submission package (SUBMISSION.md) is ready for the user to submit before 7:00 PM ET.
</success_criteria>

<output>
After completion, create `.planning/phases/12-brief-readme-demo/12-02-SUMMARY.md`. Include:
- the preflight.txt content (SURFACE, URL codes, Ollama, RECORD_RERUN, smoke counts)
- the user-provided fields (video link, destination, team; no secrets)
- the commit SHAs and push time (ET)
- the Pages run id and conclusion, the HF deploy result, and the final URL/HTTP-code table
- anything that blocked or was reported (dirty tree, CI failure, auth gate)
</output>
