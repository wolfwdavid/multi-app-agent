---
phase: 11-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/core/mcp/tools.ts
  - web/src/lib/core/mcp/server.ts
  - web/src/lib/core/mcp/index.ts
  - web/src/lib/core/mcp/server.test.ts
  - web/src/lib/core/mcp/stdio.test.ts
  - web/src/lib/core/mcp/README.md
  - web/scripts/mcp.ts
  - .vscode/mcp.json
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "An MCP client that runs initialize then tools/list over stdio gets exactly 4 tools: gap_analysis, critique_essay, plan_sprint, run_sprint. None of the 4 write tools (notion.upsertTrackerRow, calendar.createEvent, docs.createDoc, gmail.createDraft) and no /send/i tool is exposed directly"
    - "Each tool's inputSchema is generated from the zod object in the same AgentToolDef the handler runs (property keys equal Object.keys(def.input.shape))"
    - "gap_analysis {profileId:'demo'} returns structuredContent.reports deep-equal to analyzeGaps(demo, schools, { today: '2026-09-13' }) (4 reports)"
    - "plan_sprint writes nothing (world diff empty) and returns planId, a v1. planToken, and 23 actions whose tool names all exist in sprintRegistry"
    - "run_sprint rejects a missing or wrong planToken, an empty approvedIds, and an unknown planId with isError, and the world diff stays empty. With the valid token and all ids it returns report.status 'ok' (23 verified; 3 rows / 13 events / 4 drafts / 0 sent). A second plan_sprint + run_sprint on the same server returns 23 deduped"
    - "The stdio server runs in mock mode by default. Real connectors are used only when TRANSFERPILOT_MCP_MODE=real. Every stdout line is a JSON-RPC message; the ready banner and all logs go to stderr"
    - ".vscode/mcp.json launches `npx tsx scripts/mcp.ts` with cwd ${workspaceFolder}/web, so VS Code can list and call the tools"
  artifacts:
    - path: "web/src/lib/core/mcp/tools.ts"
      provides: "4 agent tool definitions (zod inputs + run) over planSprint/executeSprint/analyzeGaps/critique slot; McpDeps + session"
      exports: ["MCP_TOOL_NAMES", "createAgentTools", "createMcpDeps", "McpDeps", "AgentToolDef", "McpSession"]
    - path: "web/src/lib/core/mcp/server.ts"
      provides: "buildMcpServer(deps): McpServer with every agent tool registered from its zod input"
      exports: ["buildMcpServer"]
    - path: "web/scripts/mcp.ts"
      provides: "stdio entry: console to stderr, loads data, resolves mode/secret/today, serveStdio"
      contains: "serveStdio"
    - path: "web/src/lib/core/mcp/server.test.ts"
      provides: "in-memory JSON-RPC integration test (initialize, tools/list, tools/call for all 4 tools, approval refusals, dedupe)"
    - path: "web/src/lib/core/mcp/stdio.test.ts"
      provides: "spawned stdio handshake: initialize + tools/list (4 tools) + gap_analysis, stdout purity"
    - path: ".vscode/mcp.json"
      provides: "VS Code MCP client config"
      contains: "\"servers\""
  key_links:
    - from: "web/src/lib/core/mcp/server.ts"
      to: "web/src/lib/core/mcp/tools.ts"
      via: "for (const t of createAgentTools(deps)) server.registerTool(t.name, { inputSchema: t.input, ... })"
      pattern: "inputSchema: t\\.input"
    - from: "web/src/lib/core/mcp/tools.ts"
      to: "web/src/lib/core/agent/sprint.ts"
      via: "plan_sprint -> planSprint, run_sprint -> executeSprint (authorizeExecution is the only write path)"
      pattern: "executeSprint\\("
    - from: "web/src/lib/core/mcp/tools.ts"
      to: "web/src/lib/core/tools/sprint-tools.ts"
      via: "sprintRegistry: plan actions annotated from registry; write tools never exposed directly"
      pattern: "sprintRegistry"
    - from: "web/scripts/mcp.ts"
      to: "@modelcontextprotocol/server/stdio"
      via: "serveStdio(() => buildMcpServer(deps))"
      pattern: "serveStdio\\("
---

<objective>
Expose the agent through a custom MCP stdio server that VS Code can use. It has 4 tools: `gap_analysis`, `critique_essay`, `plan_sprint` and `run_sprint`. All four are built on the same core entry points and zod contracts the CLI, evals and REST API use. `run_sprint` writes only with a plan token from `plan_sprint` plus explicit approvedIds, and runs on mocks unless real mode is explicitly enabled.

Purpose: MCP-01. Phase 11 is a cut candidate with a 15-minute time box (5:35-5:50 PM ET, hard stop 6:15). If Phase 4's `planSprint`/`executeSprint` aren't on disk when execution starts, STOP and report (see PRE-FLIGHT).
Output: 3 core MCP modules, 2 tests, the stdio entry script, VS Code config, and a README snippet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/04-sprint-pipeline-on-mocks/04-03-SUMMARY.md

<conventions>
- PRE-FLIGHT (do first, takes about 1 minute):
  1. Check that these exist: `web/src/lib/core/agent/sprint.ts` (exports `planSprint`, `executeSprint`, `allActionIds`, `SprintRuntime`), `web/src/lib/core/agent/recording.ts` (`preflightPlan`), `web/src/lib/core/tools/sprint-tools.ts` (`sprintRegistry`, `WRITE_TOOLS`), `web/src/lib/core/agent/planner.ts` (`CritiqueSlotOutput`), `web/src/lib/core/llm/fake.ts` (`createFakeLLM`), `web/src/lib/core/llm/types.ts` (`callSlot`). If any are missing, STOP and report "Phase 4 not landed". Don't write stand-ins. If the 04-0x SUMMARYs show signatures that differ from <interfaces>, follow the SUMMARY and the code, and record the difference.
  2. Critique entry point. Run `ls .planning/phases/05-*/05-01-SUMMARY.md 2>/dev/null` and `grep -rnE "export (async )?function (critique|runCritique|buildCritique)" web/src/lib/core --include=*.ts | grep -v test`. If Phase 5 has exported a critique function, `critique_essay` calls it (per its SUMMARY signature) and returns `source: 'phase5'`. Otherwise use the placeholder in Task 1 (FakeLLM `critique` slot via `callSlot` + `CritiqueSlotOutput`), return `source: 'placeholder'`, and keep the `// PHASE5-SWAP:` comment on that line.
- SDK (VERIFIED against the installed web/node_modules/@modelcontextprotocol/server@2.0.0 during planning; don't re-guess):
  - `import { McpServer, InMemoryTransport } from '@modelcontextprotocol/server'`.
  - `import { serveStdio, StdioServerTransport } from '@modelcontextprotocol/server/stdio'` (node-only; import it ONLY in `web/scripts/mcp.ts`).
  - `server.registerTool(name, { title?, description?, inputSchema?: z.object(...), annotations? }, async (args, ctx) => CallToolResult)`. A zod 4 object is accepted directly and listed as JSON Schema with `additionalProperties: false` for `.strict()`.
  - Invalid arguments come back as `{ isError: true, content: [{ type: 'text', text: 'Input validation error: ...' }] }`. A handler that throws comes back as `{ isError: true, content: [{ type:'text', text: err.message }] }`.
  - `initialize` with `protocolVersion: '2025-06-18'` returns that version and `capabilities.tools`.
  - `serveStdio(factory, { transport?, onerror? })` serves a 2025-era `initialize` (the factory is called once per connection) and returns `{ close() }`.
  - `InMemoryTransport.createLinkedPair()` gives `[clientSide, serverSide]`; `await server.connect(serverSide); clientSide.onmessage = ...; await clientSide.start(); clientSide.send(msg)`.
  - NO client package is installed. The tests speak raw JSON-RPC. Do NOT add `@modelcontextprotocol/client` or edit `web/package.json`.
  - Fallback: `@modelcontextprotocol/sdk@1.30.0` only if v2 is blocked for more than 5 minutes. It would require a package.json change, so report it first instead of doing it silently.
- OWNERSHIP: create or edit only the files in `files_modified`. Do NOT edit `web/package.json` (run with `npx tsx`), the root README.md (Phase 12 links `web/src/lib/core/mcp/README.md`), or any Phase 1-10 file.
- Core files (`src/lib/core/mcp/*.ts`, non-test): relative `.ts` imports, no `$app/$env/$lib/svelte/@sveltejs/kit` (boundary.test.ts), no `process.env`, no `node:*`, no `console.*`, no module-level mutable state (session state lives in `createMcpDeps`' return value). Test files MAY use `node:child_process`, `node:path`, `node:url`, `node:fs`.
- stdout is the JSON-RPC channel (PITFALLS 15). In `scripts/mcp.ts`, redirect `console.log/info/debug` to `console.error` BEFORE dynamically importing core, and write the ready banner with `console.error`.
- Gap data: never assert per-school warning counts (for example "Cornell has exactly one warning"; the real snapshot has 4 non-blocker warnings). Compare `gap_analysis` output by deep equality with `analyzeGaps(...)` and by report count only.
- vitest `requireAssertions: true`: every test calls expect.
- COMMITS: commit messages must NOT contain Co-Authored-By, Claude, Claude-Session, Anthropic, or any AI-assistant trailer or mention. Describe only the change. Self-check with `git log -1 --format=%B | grep -inE "co-authored|claude|anthropic"`, which must print nothing.
</conventions>

<interfaces>
<!-- Phase 1-3 (committed code, verified) -->
From web/src/lib/core/tools/registry.ts: `ToolDef { name; app: AppName; effect: ToolEffect; description; input; output; run(ctx: { connectors }, args) }`, `createRegistry(tools)`, `Registry { list(); get(name); has(name) }`. `AppName` = gmail|calendar|docs|notion|github|hf. That is why the agent-level tools use their own `AgentToolDef` (below): they span apps. Their inputs compose the registry's contracts, and the writes still go only through `sprintRegistry` via `executeSprint`.
From web/src/lib/core/schemas.ts: `Profile` (profile_id, student, targets[{school_id, program_id, term}], contacts, essay_doc_id), `SchoolsDataset`, `Plan`, `RunReport`.
From web/src/lib/core/gap/index.ts: `analyzeGaps(profile, schools, { today }): GapReport[]` (one per target, in order). GapReport has schoolId, programId, schoolName, programName, targetTerm, aiPolicy.mode, essays.required[{essayId, prompt, wordLimit}].
From web/src/lib/core/connectors/index.ts: `createConnectors({ mode: 'mock', world? }) -> Connectors & { mode:'mock'; world: World }`; `createConnectors({ mode: 'real', env }) -> Connectors & { mode:'real'; world: null }`; `DEFAULT_CLOCK = '2026-09-13T17:00:00.000Z'`; `isEmptyDiff(d)`; `World { state; snapshot(); diff(before) }`; type `Env`.
From web/src/lib/core/connectors/types.ts: `DocsPort.readDoc(docId): Promise<{ ref: DocRef; text: string }>` (the text is UNTRUSTED; never put it in an LLM prompt here).
Demo data: profiles in `web/src/lib/core/data/profiles/{demo,demo-quarter}.json` (profile_id 'demo', 'demo-quarter'); demo essay_doc_id 'doc-essay-demo'; Berkeley programId 'uc-berkeley-data-science-ba'; Cornell 'cornell-as-economics'.

<!-- Phase 4 (from 04-01/04-03 plans; confirm against SUMMARYs) -->
```ts
// agent/sprint.ts
export interface SprintRuntime { connectors: Connectors; mode: 'mock'|'real'; llm: LLM; tracer: Tracer; planSecret: string; today: string; now?; sleep?; limits?; repairArgs? }
export function allActionIds(plan: Plan): string[];
export async function planSprint(rt, { profile, schools, createdAt }): Promise<{ plan: Plan; planToken: string; reports: GapReport[] }>;
export async function executeSprint(rt, { plan: unknown; planToken: string; approvedIds: string[]; profile: Profile; runId? }): Promise<RunReport>; // throws PolicyError { name:'PolicyError', code:'invalid_plan'|'invalid_plan_token'|'unknown_action_id', message }
// agent/recording.ts
export async function preflightPlan(connectors, plan): Promise<Record<string, 'new'|'exists'>>;
// agent/planner.ts
export const CritiqueSlotOutput: z.ZodObject<{ policyNote: string; sections: { heading; comments: string[] }[] }>;
// tools/sprint-tools.ts
export const WRITE_TOOLS: Readonly<Record<WriteToolName, WriteToolDef>>; export const sprintRegistry: Registry;
// llm/types.ts, llm/fake.ts
export async function callSlot(llm, schema, { slot, system, input }, opts?): Promise<z.infer<S>>;
export function createFakeLLM(script?): FakeLLM;  // model 'fake-scripted'
// trace/tracer.ts
export function createTracer({ sinks, traceId?, now? }): Tracer;
// Demo run on mocks (today 2026-09-13): 23 actions; after approve-all: 3 notion rows, 13 calendar events, 3 keyed docs, 4 drafts, 0 sent; rerun -> 23 deduped.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core MCP adapter: agent tool definitions (zod inputs), buildMcpServer, and an in-memory JSON-RPC integration test</name>
  <files>web/src/lib/core/mcp/tools.ts, web/src/lib/core/mcp/server.ts, web/src/lib/core/mcp/index.ts, web/src/lib/core/mcp/server.test.ts</files>
  <read_first>
    - web/src/lib/core/agent/sprint.ts, web/src/lib/core/agent/recording.ts, web/src/lib/core/agent/index.ts (actual Phase 4 exports)
    - web/src/lib/core/tools/sprint-tools.ts and web/src/lib/core/tools/registry.ts
    - web/src/lib/core/agent/planner.ts (CritiqueSlotOutput) and web/src/lib/core/llm/types.ts (callSlot), web/src/lib/core/llm/fake.ts
    - web/src/lib/core/connectors/index.ts (createConnectors, DEFAULT_CLOCK, isEmptyDiff, Env)
    - web/src/lib/core/data/profiles-fixtures.test.ts (JSON import style for tests)
    - web/src/lib/core/boundary.test.ts (core import rules)
  </read_first>
  <behavior>
    Test setup: `schools = SchoolsDataset.parse(schoolsRaw)`, `demo = Profile.parse(demoRaw)`, `demoQuarter = Profile.parse(demoQuarterRaw)`, `deps = createMcpDeps({ profiles: [demo, demoQuarter], schools, planSecret: 'mcp-test-secret-0123456789', today: '2026-09-13', clock: () => '2026-09-13T17:00:00.000Z', mode: 'mock' })`, `server = buildMcpServer(deps)`, and a raw JSON-RPC client over `InMemoryTransport.createLinkedPair()` with `request(method, params)` resolving by id. The client runs `initialize` (protocolVersion '2025-06-18', clientInfo {name:'vitest', version:'0'}) and then sends `notifications/initialized`. Helper `call(name, args)` returns `res.result`. Each `it` uses a fresh deps + server.
    - initialize: result.serverInfo.name === 'transferpilot'; result.capabilities.tools is defined.
    - tools/list:
      - names sorted deep-equal ['critique_essay','gap_analysis','plan_sprint','run_sprint']
      - no name is in Object.keys(WRITE_TOOLS); no name matches /send/i
      - every inputSchema.type === 'object'
      - for each def in createAgentTools(deps): Object.keys(listed.inputSchema.properties).sort() deep-equals Object.keys(def.input.shape).sort()
      - run_sprint inputSchema.required includes 'planId', 'planToken', 'approvedIds'
      - gap_analysis inputSchema.properties.profileId.enum includes 'demo' and 'demo-quarter'
    - gap_analysis {profileId:'demo'}: not isError; structuredContent.reports deep-equals JSON.parse(JSON.stringify(analyzeGaps(demo, schools, { today: '2026-09-13' }))); reports.length 4; JSON.parse(content[0].text).reports.length 4.
    - gap_analysis {profileId:'demo', programId:'cornell-as-economics'} → exactly 1 report with that programId. {profileId:'nobody'} → isError (SDK input validation). {profileId:'demo', programId:'nope'} → isError, text contains 'nope'.
    - critique_essay {profileId:'demo', programId:'uc-berkeley-data-science-ba'}: not isError; structuredContent.essayDocId === 'doc-essay-demo'; typeof essayWordCount === 'number' and > 0; critique.sections.length >= 1; source is 'placeholder' or 'phase5'; the world diff against the pre-call snapshot is empty. {programId:'nope'} → isError.
    - plan_sprint {profileId:'demo'}: not isError; planId matches /^plan-/; planToken matches /^v1\./; actions.length 23; every action.tool satisfies sprintRegistry.has; every action.preflight === 'new'; blockers include code 'NO_TRANSFER_PROGRAM'; the world diff is empty.
    - run_sprint refusals, each isError with an empty world diff:
      - {planId, approvedIds: all} without planToken
      - {planId, planToken, approvedIds: []}
      - {planId, planToken: 'v1.' + 'A'.repeat(43), approvedIds: all} → text contains 'invalid_plan_token'
      - {planId: 'plan-000000000000', planToken, approvedIds: all} → text contains 'plan_sprint'
      - {planId, planToken, approvedIds: ['x.y']} → text contains 'unknown_action_id'
    - run_sprint happy path: approve all 23 → not isError; structuredContent.report.status 'ok'; report.counts.verified 23; structuredContent.world deep-equals { notionRows: 3, calendarEvents: 13, docs: 3, gmailDrafts: 4, gmailSent: 0 }.
    - Idempotent via MCP: on the same server, call plan_sprint again, then run_sprint with the new planId/token and all ids → report.counts.deduped 23; world counts unchanged.
    - Partial approval: approve all except 'cornell-as-economics.draft_rec_request' → that artifact's status is 'skipped'; world.gmailDrafts 3.
  </behavior>
  <action>
1. `web/src/lib/core/mcp/tools.ts`, imports: `z` from 'zod'; `Profile`, `SchoolsDataset`, `Plan` types from `../schemas.ts`; `analyzeGaps` from `../gap/index.ts`; `createConnectors`, `type Env`, `type World` from `../connectors/index.ts`; `planSprint`, `executeSprint`, `type SprintRuntime` from `../agent/sprint.ts`; `preflightPlan` from `../agent/recording.ts`; `CritiqueSlotOutput` from `../agent/planner.ts`; `sprintRegistry` from `../tools/sprint-tools.ts`; `callSlot` from `../llm/types.ts`; `createFakeLLM` from `../llm/fake.ts`; `type LLM` from `../llm/types.ts`; `createTracer` from `../trace/tracer.ts`.
```ts
export const MCP_TOOL_NAMES = ['gap_analysis', 'critique_essay', 'plan_sprint', 'run_sprint'] as const;
export type McpToolName = (typeof MCP_TOOL_NAMES)[number];
export interface McpSession { plans: Map<string, { plan: Plan; profileId: string }> }
export interface McpDeps {
  profiles: Readonly<Record<string, Profile>>;   // keyed by profile_id
  schools: SchoolsDataset;
  runtime: SprintRuntime;                        // connectors persist for the server process -> reruns dedupe
  world: World | null;                           // mock world for counts; null in real mode
  clock: () => string;                           // ISO createdAt provider, injected by the entry point
  session: McpSession;                           // plans issued by plan_sprint in this process
}
export interface AgentToolDef<I extends z.ZodObject = z.ZodObject> {
  name: McpToolName; title: string; description: string; effect: 'read' | 'write';
  input: I;
  run(deps: McpDeps, args: z.infer<I>): Promise<Record<string, unknown>>;
}
export function createMcpDeps(opts: { profiles: Profile[]; schools: SchoolsDataset; planSecret: string; today: string; clock: () => string; mode: 'mock' | 'real'; env?: Env; llm?: LLM }): McpDeps
export function createAgentTools(deps: McpDeps): AgentToolDef[]   // exactly 4, in MCP_TOOL_NAMES order
```
   - createMcpDeps:
     - `bundle = opts.mode === 'real' ? createConnectors({ mode: 'real', env: opts.env ?? {} }) : createConnectors({ mode: 'mock' })`
     - runtime: `{ connectors: bundle, mode: opts.mode, llm: opts.llm ?? createFakeLLM(), tracer: createTracer({ sinks: [], traceId: 'mcp' }), planSecret, today }`
     - `profiles` = a record keyed by profile_id; throw on a duplicate id
     - `world: bundle.world`, `session: { plans: new Map() }`
     - Mock mode is the default at the entry point. This function never reads env.
   - Shared zod pieces, built inside createAgentTools from deps:
     - `ProfileId = z.enum(Object.keys(deps.profiles) as [string, ...string[]]).describe('Profile id, e.g. demo')`
     - `ProgramId = z.string().min(1).max(200).describe('Target program id from the profile, e.g. uc-berkeley-data-science-ba')`
     - Helpers: `reportFor(profile, programId)` runs analyzeGaps and finds the report, else throws `Error(`unknown programId "${programId}" for profile ${profile_id}; targets: ${ids.join(', ')}`)`. `worldCounts(deps)` returns `deps.world ? { notionRows: s.notion.rows.length, calendarEvents: s.calendar.events.length, docs: s.docs.docs.filter(d => d.key).length, gmailDrafts: s.gmail.drafts.length, gmailSent: s.gmail.sent.length } : null`.
   - Tool 1, `gap_analysis` (effect 'read'):
     - input: `z.object({ profileId: ProfileId, programId: ProgramId.optional(), today: z.iso.date().optional().describe('YYYY-MM-DD; defaults to the server date') }).strict()`
     - run: `reports = analyzeGaps(profile, schools, { today: args.today ?? rt.today })`, filtered by programId when given (empty result → throw the unknown-programId error)
     - returns `{ profileId, today, reports }`
     - description: 'Deterministic per-school transfer gap report (GPA, units, prerequisites, essays, recommendations, deadlines, feasibility warnings) with source links. Read-only.'
   - Tool 2, `critique_essay` (effect 'read'):
     - input: `z.object({ profileId: ProfileId, programId: ProgramId }).strict()`
     - run:
       - `report = reportFor(...)`
       - `essay = await rt.connectors.docs.readDoc(profile.essay_doc_id)`. Only the profile's specified doc is read. The text is untrusted: use it ONLY for `essayWordCount = essay.text.trim() ? essay.text.trim().split(/\s+/).length : 0`, never inside an LLM input.
       - If `essayWordCount === 0`, throw `Error('essay doc is empty; nothing to critique')`.
       - `// PHASE5-SWAP: replace with the Phase 5 critique entrypoint when it lands`, then `critique = await callSlot(rt.llm, CritiqueSlotOutput, { slot: 'critique', system: 'You write question-style essay feedback. Never rewrite prose.', input: { schoolName, programName, term: report.targetTerm, aiPolicyMode: report.aiPolicy.mode, essays: report.essays.required.map(e => ({ essayId: e.essayId, prompt: e.prompt, wordLimit: e.wordLimit })) } })`
       - returns `{ profileId, programId, essayDocId: profile.essay_doc_id, essayWordCount, wordLimits: report.essays.required.map(e => ({ essayId: e.essayId, wordLimit: e.wordLimit })), aiPolicyMode: report.aiPolicy.mode, critique, source: 'placeholder' }`
       - If PRE-FLIGHT step 2 found a Phase 5 export, call it instead and set source 'phase5'.
     - description: 'Question-style critique of the profile essay against a target program prompt and AI policy. Reads only the profile essay doc; writes nothing (the critique Doc is written by run_sprint).'
   - Tool 3, `plan_sprint` (effect 'read', dry run):
     - input: `z.object({ profileId: ProfileId }).strict()`
     - run:
       - `{ plan, planToken } = await planSprint(rt, { profile, schools, createdAt: deps.clock() })`
       - `preflight = await preflightPlan(rt.connectors, plan)`
       - `deps.session.plans.set(plan.planId, { plan, profileId })`
       - returns `{ planId, planToken, profileId, mode: rt.mode, actions: plan.actions.map(a => ({ id: a.id, app: a.app, tool: a.tool, toolDescription: sprintRegistry.get(a.tool)?.description ?? null, summary: a.summary, idempotencyKey: a.idempotencyKey, dependsOn: a.dependsOn, preflight: preflight[a.id], preview: previewPayload(a) })), blockers: plan.blockers, next: 'Nothing has been written. To execute, call run_sprint with this planId, planToken and the approvedIds you accept.' }`
     - previewPayload (a local pure function):
       - gmail: `{ to, subject }`
       - calendar: `{ date, title }`
       - notion: `{ title, deadline, status }`
       - docs: `{ title }`
       - otherwise `{}`
     - description: 'Build a dry-run application sprint plan (critique Doc, Notion tracker rows, Calendar deadlines/reminders, Gmail drafts) with idempotency keys and a signed plan token. Writes nothing.'
   - Tool 4, `run_sprint` (effect 'write'):
     - input: `z.object({ planId: z.string().min(1).max(64), planToken: z.string().min(4).max(200).describe('planToken returned by plan_sprint'), approvedIds: z.array(z.string().min(1).max(200)).min(1).max(100).describe('Action ids from plan_sprint that the user approved') }).strict()`
     - run:
       - `entry = deps.session.plans.get(args.planId)`. If missing, throw `Error(`unknown planId "${planId}"; call plan_sprint first`)`.
       - In try/catch: `report = await executeSprint(rt, { plan: entry.plan, planToken: args.planToken, approvedIds: args.approvedIds, profile: deps.profiles[entry.profileId] })`. On an error whose name is 'PolicyError', throw `Error(`REJECTED ${code}: ${message}`)`. Rethrow anything else.
       - returns `{ planId, mode: rt.mode, report, world: worldCounts(deps) }`
     - The token is verified in core by authorizeExecution (HMAC over plan + recipient allowlist). Unapproved ids are skipped, and there is no send tool at any layer.
     - description: 'Execute ONLY the approved actions of a plan from plan_sprint. Requires planToken and approvedIds; tampered plans, bad tokens and unknown ids are rejected before any write. Mock apps unless the server runs in real mode. Gmail is drafts-only.'
   - Header comment: agent-level MCP tools span several apps, so they aren't registry ToolDefs (AppName is single-app). Their writes go ONLY through executeSprint → authorizeExecution → sprintRegistry write tools, and the 4 write tools are intentionally never exposed as standalone MCP tools.
2. `web/src/lib/core/mcp/server.ts`:
```ts
import { McpServer } from '@modelcontextprotocol/server';
export const MCP_SERVER_INFO = { name: 'transferpilot', version: '0.1.0' } as const;
export function buildMcpServer(deps: McpDeps): McpServer
```
   - `server = new McpServer({ ...MCP_SERVER_INFO })`. For each `t of createAgentTools(deps)`: `server.registerTool(t.name, { title: t.title, description: t.description, inputSchema: t.input, annotations: { readOnlyHint: t.effect === 'read', destructiveHint: false, idempotentHint: true, openWorldHint: deps.runtime.mode === 'real' } }, async (args) => { const data = await t.run(deps, args); return { content: [{ type: 'text', text: JSON.stringify(data, null, 2) }], structuredContent: data }; })`.
   - Let thrown errors propagate: the SDK turns them into `isError` results (verified).
   - If generic inference fights the heterogeneous loop, type the list as `AgentToolDef<any>[]` using the same `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- …` pattern as registry.ts. No other `any`.
3. `web/src/lib/core/mcp/index.ts`: `export * from './tools.ts'; export * from './server.ts';`
4. `web/src/lib/core/mcp/server.test.ts`: every <behavior> bullet (at least 9 `it`s).
   - Raw client helper: `async function connect(server) { const [c, s] = InMemoryTransport.createLinkedPair(); const pending = new Map<number, (m: any) => void>(); c.onmessage = (m: any) => { if ('id' in m && pending.has(m.id)) { pending.get(m.id)!(m); pending.delete(m.id); } }; await server.connect(s); await c.start(); let id = 0; const request = (method: string, params: unknown) => new Promise<any>((r) => { const i = ++id; pending.set(i, r); void c.send({ jsonrpc: '2.0', id: i, method, params } as never); }); ... }`
   - Snapshot `deps.world!.snapshot()` before calls, and check `isEmptyDiff(deps.world!.diff(snap))`.
   - Write the tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test -- src/lib/core/mcp/server.test.ts src/lib/core/boundary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with >= 9 passing tests in server.test.ts, and boundary.test.ts still passes
    - `grep -c "inputSchema: t.input" web/src/lib/core/mcp/server.ts` == 1
    - `grep -cE "'(gap_analysis|critique_essay|plan_sprint|run_sprint)'" web/src/lib/core/mcp/tools.ts` >= 4
    - `grep -c "executeSprint(" web/src/lib/core/mcp/tools.ts` == 1 and `grep -c "planSprint(" web/src/lib/core/mcp/tools.ts` == 1 and `grep -c "sprintRegistry" web/src/lib/core/mcp/tools.ts` >= 1
    - `grep -c "PHASE5-SWAP" web/src/lib/core/mcp/tools.ts` >= 1 (or the SUMMARY records that Phase 5's critique entry point was wired and source is 'phase5')
    - `grep -cE "process\.env|from 'node:|console\.|@modelcontextprotocol/server/stdio" web/src/lib/core/mcp/tools.ts web/src/lib/core/mcp/server.ts web/src/lib/core/mcp/index.ts` totals 0
    - `grep -cE "invalid_plan_token|unknown_action_id|deduped" web/src/lib/core/mcp/server.test.ts` >= 3 and `grep -c "WRITE_TOOLS" web/src/lib/core/mcp/server.test.ts` >= 1
    - `git diff --name-only -- web/package.json` prints nothing
  </acceptance_criteria>
  <done>An in-process MCP server exposes exactly the 4 agent tools, with JSON input schemas generated from their zod definitions. gap_analysis matches analyzeGaps, and plan_sprint is a write-free dry run. run_sprint refuses without a valid token and approvedIds, executes approved actions on mocks, and dedupes on rerun. All of it is proven over real JSON-RPC.</done>
</task>

<task type="auto">
  <name>Task 2: stdio entry script (stderr-only logging, mock default), spawned stdio handshake test, VS Code config and README snippet</name>
  <files>web/scripts/mcp.ts, web/src/lib/core/mcp/stdio.test.ts, .vscode/mcp.json, web/src/lib/core/mcp/README.md</files>
  <read_first>
    - web/src/lib/core/mcp/tools.ts, web/src/lib/core/mcp/server.ts (Task 1)
    - web/scripts/build-schools.ts (script conventions: node:fs, fileURLToPath(new URL(..., import.meta.url)))
    - web/scripts/sprint.ts if present (PLAN_SIGNING_SECRET handling and today pinning from 04-03)
    - .planning/research/PITFALLS.md Pitfall 15 (stdio stdout rule, Windows npx/cmd, spaces in path)
  </read_first>
  <action>
1. `web/scripts/mcp.ts`. The header doc comment holds usage `npx tsx scripts/mcp.ts` (run from `web/`), the env vars (`TRANSFERPILOT_MCP_MODE=mock|real`, default mock; `PLAN_SIGNING_SECRET`; `TRANSFERPILOT_TODAY=YYYY-MM-DD`), and the stdout rule.
   - Static imports ONLY: `node:fs` (readFileSync, readdirSync), `node:path` (join), `node:url` (fileURLToPath), `serveStdio` from `@modelcontextprotocol/server/stdio`.
   - First statements: `const toStderr = (...a: unknown[]) => console.error(...a); console.log = toStderr; console.info = toStderr; console.debug = toStderr;` with the comment `// stdout is the MCP JSON-RPC channel; any stray write corrupts the stream`.
   - Then dynamic imports: `const { buildMcpServer, createMcpDeps } = await import('../src/lib/core/mcp/index.ts'); const { Profile, SchoolsDataset } = await import('../src/lib/core/schemas.ts'); const { DEFAULT_CLOCK } = await import('../src/lib/core/connectors/index.ts');`
   - `WEB = fileURLToPath(new URL('..', import.meta.url))`.
     - Profiles: every `*.json` in `join(WEB, 'src/lib/core/data/profiles')`, run through `Profile.parse(JSON.parse(readFileSync(...,'utf8')))`.
     - Schools: `SchoolsDataset.parse(JSON.parse(readFileSync(join(WEB, 'src/lib/core/data/schools.json'), 'utf8')))`.
   - Mode:
     - Read `raw = (process.env.TRANSFERPILOT_MCP_MODE ?? '').trim().toLowerCase()`. '' or 'mock' → 'mock'; 'real' → 'real' with `console.error('WARNING: real mode — run_sprint writes to configured external apps (still requires planToken + approvedIds)')`.
     - Any other value → `console.error(`invalid TRANSFERPILOT_MCP_MODE "${raw}" (expected mock|real)`)` and `process.exit(64)`.
     - Real mode passes `env: process.env as Record<string, string | undefined>` (cast to the `Env` type).
   - Secret: `process.env.PLAN_SIGNING_SECRET` when its length is >= 16. Otherwise `globalThis.crypto.randomUUID() + globalThis.crypto.randomUUID()`, with `console.error('PLAN_SIGNING_SECRET not set: using a per-process secret (plan tokens valid for this server session only)')`.
   - today: `process.env.TRANSFERPILOT_TODAY` if it matches /^\d{4}-\d{2}-\d{2}$/, else `DEFAULT_CLOCK.slice(0, 10)`. Pinned like the sprint CLI so demo output reproduces.
   - `deps = createMcpDeps({ profiles, schools, planSecret, today, clock: () => new Date().toISOString(), mode, ...(mode === 'real' ? { env } : {}) })`. Create deps ONCE outside the factory, so the mock world and plan session survive for the whole connection.
   - `const handle = serveStdio(() => buildMcpServer(deps), { onerror: (e) => console.error('mcp error:', e.message) });`
   - `console.error(`transferpilot MCP server ready (mode=${mode}, today=${today}, profiles=${Object.keys(deps.profiles).join(',')}, tools=gap_analysis,critique_essay,plan_sprint,run_sprint)`);`
   - `const shutdown = () => { void handle.close().finally(() => process.exit(0)); }; process.stdin.on('end', shutdown); process.on('SIGINT', shutdown); process.on('SIGTERM', shutdown);`
2. `web/src/lib/core/mcp/stdio.test.ts`: one `it` with a 60_000 ms timeout.
   - `WEB = fileURLToPath(new URL('../../../../', import.meta.url))`.
   - `child = spawn(process.execPath, [join(WEB, 'node_modules/tsx/dist/cli.mjs'), join(WEB, 'scripts/mcp.ts')], { cwd: WEB, env: { ...process.env, PLAN_SIGNING_SECRET: 'stdio-test-secret-0123456789', TRANSFERPILOT_MCP_MODE: '' }, stdio: ['pipe', 'pipe', 'pipe'] })`. Use no shell, which is safe with spaces in the path.
   - Collect stdout into a line buffer and resolve pending requests by id. Collect stderr into a string.
   - Send, newline-delimited:
     1. `initialize` (protocolVersion '2025-06-18', clientInfo {name:'stdio-test', version:'0'}, capabilities {})
     2. `notifications/initialized`
     3. `tools/list`
     4. `tools/call` gap_analysis {profileId:'demo'}
   - Assert:
     - initialize result.serverInfo.name === 'transferpilot'
     - tools/list names, sorted, deep-equal ['critique_essay','gap_analysis','plan_sprint','run_sprint']
     - the gap_analysis result has no isError and `structuredContent.reports.length === 4`
     - EVERY non-empty stdout line passes `JSON.parse` and has `jsonrpc === '2.0'` (stdout purity)
     - stderr contains 'transferpilot MCP server ready (mode=mock'
   - Finally `child.stdin.end(); child.kill()` in a `finally`.
   - Cap every request wait with a timer; on timeout, reject with the collected stderr for diagnosis.
3. `.vscode/mcp.json` (repo root; verified VS Code schema with top-level "servers", type stdio, command/args/cwd/env, `${workspaceFolder}` supported):
```json
{
  "servers": {
    "transferpilot": {
      "type": "stdio",
      "command": "npx",
      "args": ["tsx", "scripts/mcp.ts"],
      "cwd": "${workspaceFolder}/web",
      "env": { "TRANSFERPILOT_MCP_MODE": "mock" }
    }
  }
}
```
   cwd MUST be `web/`: npx resolves the local tsx from there. From the repo root npx would try to download tsx and block on a prompt.
4. `web/src/lib/core/mcp/README.md` (short; Phase 12 links it from the root README):
   - What it is: 4 tools and their one-line purposes.
   - The approval flow: plan_sprint → review actions → run_sprint with planToken + approvedIds; nothing is written before that; drafts only; mock by default.
   - The `.vscode/mcp.json` snippet above.
   - A Windows fallback if VS Code cannot resolve `npx`: `"command": "cmd", "args": ["/c", "npx", "tsx", "scripts/mcp.ts"]`.
   - Manual run: `cd web && npx tsx scripts/mcp.ts`. Inspector: `npx @modelcontextprotocol/inspector npx tsx scripts/mcp.ts` from `web/`.
   - Env vars: TRANSFERPILOT_MCP_MODE, PLAN_SIGNING_SECRET, TRANSFERPILOT_TODAY.
   - Note: logs go to stderr only.
   - No secrets in the file.
5. Run the full gate (see verify). If `npm --prefix web run check` flags the dynamic `.ts` imports in `scripts/` (scripts are normally outside the svelte-check include), leave the script's behavior as is and record it. Don't edit tsconfig.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test -- src/lib/core/mcp && npm --prefix web test && npm --prefix web run check && node -e "const c=JSON.parse(require('fs').readFileSync('.vscode/mcp.json','utf8')).servers.transferpilot; if(c.type!=='stdio'||c.command!=='npx'||c.args.join(' ')!=='tsx scripts/mcp.ts'||c.cwd!=='\${workspaceFolder}/web')process.exit(1); console.log('VSCODE_CFG_OK')" && cd web && node -e "
const {spawn}=require('child_process');const p=spawn(process.execPath,['node_modules/tsx/dist/cli.mjs','scripts/mcp.ts'],{stdio:['pipe','pipe','pipe']});let out='';const t=setTimeout(()=>{console.error('TIMEOUT');p.kill();process.exit(1)},45000);
p.stdout.on('data',d=>{out+=d;const lines=out.split('\n').filter(Boolean);const list=lines.map(l=>JSON.parse(l)).find(m=>m.id===2);if(list){const n=list.result.tools.map(t=>t.name).sort().join(',');clearTimeout(t);p.kill();if(n!=='critique_essay,gap_analysis,plan_sprint,run_sprint'){console.error('BAD',n);process.exit(1)}console.log('STDIO_HANDSHAKE_OK 4 tools');process.exit(0)}});
const w=m=>p.stdin.write(JSON.stringify(m)+'\n');w({jsonrpc:'2.0',id:1,method:'initialize',params:{protocolVersion:'2025-06-18',capabilities:{},clientInfo:{name:'gate',version:'0'}}});setTimeout(()=>{w({jsonrpc:'2.0',method:'notifications/initialized'});w({jsonrpc:'2.0',id:2,method:'tools/list',params:{}})},1500);"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web test -- src/lib/core/mcp` exits 0 (server.test.ts plus stdio.test.ts)
    - `npm --prefix web test` exits 0 (full suite) and `npm --prefix web run check` exits 0
    - The scripted handshake in verify prints `STDIO_HANDSHAKE_OK 4 tools`, and the config check prints `VSCODE_CFG_OK`
    - `grep -c "serveStdio(" web/scripts/mcp.ts` == 1 and `grep -c "console.log = " web/scripts/mcp.ts` == 1 and `grep -c "console.log(" web/scripts/mcp.ts` == 0
    - `grep -c "TRANSFERPILOT_MCP_MODE" web/scripts/mcp.ts` >= 1 and `grep -c "'mock'" web/scripts/mcp.ts` >= 1 (mock is the default; real requires the explicit flag)
    - `grep -c "PLAN_SIGNING_SECRET" web/scripts/mcp.ts` >= 1
    - `grep -c "transferpilot MCP server ready" web/src/lib/core/mcp/stdio.test.ts` == 1 and `grep -c "jsonrpc" web/src/lib/core/mcp/stdio.test.ts` >= 2
    - `grep -c "\"cwd\": \"\${workspaceFolder}/web\"" .vscode/mcp.json` == 1
    - `grep -cE "plan_sprint|run_sprint|cmd" web/src/lib/core/mcp/README.md` >= 3
    - `git diff --name-only -- web/package.json README.md` prints nothing
  </acceptance_criteria>
  <done>`npx tsx scripts/mcp.ts` (cwd web/) serves the 4 tools over stdio in mock mode with pure JSON-RPC on stdout and logs on stderr. A spawned-process test and a scripted handshake both see 4 tools and a working gap_analysis. VS Code is configured through .vscode/mcp.json, and the README snippet documents the approval flow and the Windows fallback.</done>
</task>

</tasks>

<verification>
Phase 11 gate, run sequentially from Git Bash at the repo root:
1. `npm --prefix web test`: the full suite is green, including `src/lib/core/mcp/server.test.ts`, `src/lib/core/mcp/stdio.test.ts` and `boundary.test.ts`.
2. `npm --prefix web run check`: 0 errors.
3. The scripted stdio handshake (Task 2 verify) prints `STDIO_HANDSHAKE_OK 4 tools`.
4. `grep -rnE "process\.env|from 'node:|console\." web/src/lib/core/mcp --include=*.ts | grep -v "\.test\.ts"` returns nothing.
5. `git diff --name-only -- web/package.json` is empty.
6. Optional manual check if time allows (not a gate): open the repo in VS Code, then Command Palette → "MCP: List Servers" → transferpilot → Start. Its tools show up in Copilot agent mode, and gap_analysis {profileId: demo} returns 4 reports.
</verification>

<success_criteria>
- ROADMAP Phase 11 criterion 1: VS Code config lists gap_analysis, critique_essay, plan_sprint and run_sprint from the stdio server, and each input schema comes from its zod definition (the tools/list test asserts property parity).
- Criterion 2: gap_analysis equals analyzeGaps for the demo profile. run_sprint runs in mock mode by default and refuses (isError, no world change) without a valid planToken and approvedIds, or with an unknown planId or action id. An approved run verifies 23 artifacts, and a rerun dedupes all 23.
- Criterion 3: every stdout line in the spawned-server test is valid JSON-RPC, and logs and the ready banner go to stderr.
- MCP-01 satisfied; package.json untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mcp-server/11-01-SUMMARY.md`. Include:
- the tool table: name, input fields, effect, what it calls
- the VS Code setup snippet and Windows fallback
- whether critique_essay uses the placeholder (PHASE5-SWAP) or the Phase 5 entry point
- any Phase 4 signature differences found in PRE-FLIGHT
- the stdio handshake output line
</output>
