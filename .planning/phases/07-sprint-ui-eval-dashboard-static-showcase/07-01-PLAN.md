---
phase: 07-sprint-ui-eval-dashboard-static-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/routes/layout.css
  - web/src/routes/+layout.ts
  - web/src/routes/+layout.svelte
  - web/src/routes/+page.svelte
  - web/src/routes/evals/+page.svelte
  - web/src/lib/ui/config.ts
  - web/src/lib/ui/status.ts
  - web/src/lib/ui/format.ts
  - web/src/lib/ui/dataset.ts
  - web/src/lib/ui/provenance.ts
  - web/src/lib/ui/profile-form.ts
  - web/src/lib/ui/data.ts
  - web/src/lib/ui/mode.ts
  - web/src/lib/ui/mode.svelte.ts
  - web/src/lib/ui/foundation.test.ts
  - web/src/lib/components/AppHeader.svelte
  - web/src/lib/components/ModeBanner.svelte
  - web/src/lib/components/StatusChip.svelte
  - web/src/lib/components/ConfidenceBadge.svelte
  - web/src/lib/components/SourceLink.svelte
  - web/src/lib/components/EmptyState.svelte
  - web/src/lib/components/ProfilePanel.svelte
  - web/src/lib/components/GapReportCard.svelte
autonomous: true
requirements: [DATA-03, UI-01]

must_haves:
  truths:
    - "Every page uses the 07-UI-SPEC semantic tokens (bg/surface/border/fg/accent + ok/warn/info/block/fail/idle) in light and dark mode. No page contains the 'Welcome to SvelteKit' placeholder"
    - "Header nav links (Sprint, Evals) and static fetches are prefixed with `base`; `trailingSlash = 'always'` makes the static build emit `index.html` and `evals/index.html`, which resolve under /multi-app-agent"
    - "On a static host (no JSON `/api/health` with target 'vercel', or `?replay` in the URL), the header pill reads 'Recorded run' and the replay banner shows the recording date + short commit SHA from data/hero-run.json"
    - "A user can pick the demo (semester) or demo-quarter profile, or edit GPA/units/unit system/terms/major/targets fields or the raw JSON. Zod errors appear inline under the matching field with aria-invalid, plus a summary; a JSON syntax error shows line and column; the gap report keeps the last valid result with 'Showing results for the last valid profile.'"
    - "The gap report renders one card per target, computed client-side by analyzeGaps with the browser's local date. Each requirement row, warning and prerequisite shows a 'Verify on official page' link (or 'Fictional school: no official page'), 'Retrieved {date}', and a confidence badge"
  artifacts:
    - path: "web/src/routes/layout.css"
      provides: "Exact 07-UI-SPEC token block (@theme inline + light/dark vars)"
      contains: "--color-ok-bg: var(--tp-ok-bg)"
    - path: "web/src/routes/+layout.ts"
      provides: "prerender + trailingSlash"
      contains: "trailingSlash = 'always'"
    - path: "web/src/lib/ui/status.ts"
      provides: "single status → {tone, glyph, label} map + literal Tailwind tone classes"
      exports: ["TONE_CLASSES", "traceChip", "artifactChip", "prereqChip", "severityChip", "gpaChip", "unitsChip", "deadlineChip", "confidenceChip", "rateTone", "runStatusChip"]
    - path: "web/src/lib/ui/provenance.ts"
      provides: "requirementId → SourceRef lookup (source_url, retrieved_at, confidence, note)"
      exports: ["lookupSource", "parseRequirementId"]
    - path: "web/src/lib/ui/profile-form.ts"
      provides: "JSON/zod profile parsing with line/column + field-path helpers"
      exports: ["parseProfile", "setField", "toggleTarget", "targetOptions", "fieldIssues", "unmappedIssues"]
    - path: "web/src/lib/ui/data.ts"
      provides: "loadStatic + HeroRunFile schema (client-side, no agent/connector imports)"
      exports: ["loadStatic", "fromSchema", "HeroRunFile"]
    - path: "web/src/lib/ui/mode.ts"
      provides: "pure detectMode (replay param, /api/health probe with timeout)"
      exports: ["detectMode"]
    - path: "web/src/lib/components/SourceLink.svelte"
      provides: "Verify link + retrieved date + confidence badge"
    - path: "web/src/lib/components/ProfilePanel.svelte"
      provides: "profile picker, fields, JSON editor, inline zod errors"
    - path: "web/src/lib/components/GapReportCard.svelte"
      provides: "per-target gap report card"
  key_links:
    - from: "web/src/routes/+page.svelte"
      to: "web/src/lib/core/gap/index.ts"
      via: "$derived analyzeGaps(profile, DATASET, { today })"
      pattern: "analyzeGaps\\("
    - from: "web/src/lib/components/SourceLink.svelte"
      to: "web/src/lib/ui/provenance.ts"
      via: "lookupSource(DATASET, requirementId)"
      pattern: "lookupSource\\("
    - from: "web/src/routes/+layout.svelte"
      to: "web/src/lib/ui/mode.svelte.ts"
      via: "initApp(page.url, base) in onMount"
      pattern: "initApp\\("
    - from: "web/src/lib/ui/mode.svelte.ts"
      to: "static/data/hero-run.json"
      via: "loadStatic(`${base}/data/hero-run.json`, fromSchema(HeroRunFile))"
      pattern: "hero-run\\.json|DATA_PATHS\\.hero"
---

<objective>
Build the Phase 7 foundation and the first half of the sprint page:
1. The UI-SPEC design tokens.
2. The global shell: skip link, header, mode detection, replay banner, `trailingSlash`, base-prefixed links.
3. Pure, tested `lib/ui` modules: status map, formatters, provenance lookup, profile form parsing, static loader, mode detection.
4. Shared components.
5. Sprint steps 1–2: the profile panel with inline zod validation (DATA-03) and client-side gap report cards whose "Verify on official page" links carry the retrieved date and confidence (the DATA-02 UI portion).

Purpose: ROADMAP Phase 7 success criterion 1 and the base of UI-01. Plan 07-02 builds steps 3–5 and the eval dashboard on these modules and components. Plan 07-03 deploys.
Output: tokens/layout/routes, 10 `lib/ui` modules plus one test file, and 8 components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md
@.planning/phases/04-sprint-pipeline-on-mocks/04-03-SUMMARY.md

<conventions>
- **07-UI-SPEC.md is the approved design contract.** Copy text, class lists, tokens, a11y rules and layout come from it verbatim. When this plan says "per UI-SPEC §X", open that section and implement it exactly.
- OWNERSHIP: create or edit only the files in `files_modified`.
  - Do NOT touch `web/src/lib/core/**`, `web/src/routes/api/**` (Phases 9/10), or `web/package.json`.
  - No new dependencies. Use only Svelte 5 runes, Tailwind v4 utilities, and zod.
- Svelte 5 runes only: `$props`, `$state`, `$derived`, `$effect`, `$bindable`; `page` from `$app/state`; `base` from `$app/paths`.
  - If svelte-check reports `base` as an *error* (not a deprecation hint), switch to `resolve()`/`asset()` from `$app/paths` with identical output URLs and note it in the SUMMARY.
- Imports:
  - `web/src/lib/ui/*.ts` use RELATIVE imports with `.ts` extensions (e.g. `../core/schemas.ts`), matching the core convention, so vitest (node project) runs them without SvelteKit aliases.
  - `.svelte` files use `$lib/...` WITHOUT a file extension (`$lib/ui/status`, `$lib/core/gap`, `$lib/ui/mode.svelte`).
- `web/src/lib/ui/*.ts` (except `mode.svelte.ts`) must not import `$app/*`, `$env/*`, or `svelte`; they are pure and unit-tested. `base` is passed in as a parameter.
- Tailwind v4 scans source files for class names, so every class string must appear LITERALLY in source (e.g. `'bg-ok-bg text-ok-fg border-ok-border'`). Never build class names by string concatenation like `` `bg-${tone}-bg` ``.
- Never hard-code a root-absolute internal URL. Internal links are `{base}/` and `{base}/evals/`; fetches are `` `${base}/data/...` ``; `/api/health` is fetched as `` `${base}/api/health` `` WITHOUT a trailing slash (`+server.ts` routes do not inherit the layout's trailingSlash).
- Git Bash converts `BASE_PATH=/multi-app-agent` to a Windows path. Always prefix the static build with `MSYS_NO_PATHCONV=1` (verified).
- COMMITS: commit messages must NOT contain `Co-Authored-By`, `Claude`, `Claude-Session`, `Generated with`, or any AI-assistant mention or trailer. Self-check after each commit with `git log -1 --format=%B | grep -inE "co-authored|claude|anthropic"`, which must print nothing. If it prints anything, amend the message. (tasks/lessons.md)
</conventions>

<interfaces>
<!-- Extracted from the codebase. Use directly. -->
From web/src/lib/core/schemas.ts:
```ts
export const Plan, Action, Blocker, ContentFlag, TraceEvent, RunReport, ArtifactStatus, FailureClass, AppName; // zod + types
// TraceEvent { traceId; spanId; parentSpanId?; name; kind: 'start'|'end'|'event'; ts; durationMs?; status?: 'ok'|'error'|'deduped'|'skipped'; attrs: Record<string, unknown> }
// RunReport { runId; traceId; planId; mode; startedAt; finishedAt; approvedIds; artifacts: ArtifactResult[]; blockers; flags; counts: {verified, deduped, failed, skipped}; status: 'ok'|'partial'|'failed' }
export * from './schemas/school.ts';  // SchoolsDataset, Program, SourceRef {source_url ('https://…' | 'about:fictional'), additional_source_urls, retrieved_at (YYYY-MM-DD), confidence: 'HIGH'|'MEDIUM'|'LOW', note?}, Confidence, UnitSystem, requirementIds(p), SCALAR_REQUIREMENT_FIELDS
export * from './schemas/profile.ts'; // Profile (zod object + superRefine): profile_id, student{name,email}, current_school, intended_major, gpa 0..4, units{completed, in_progress, system}, terms_remaining int, courses[], activities[], goals, targets[{school_id, program_id, term}] min 1, contacts[], portfolio, essay_doc_id
```
From web/src/lib/core/gap/types.ts (re-exported by `web/src/lib/core/gap/index.ts`, which also exports `analyzeGaps(profile, schools: SchoolsDataset | School[], { today }): GapReport[]`):
```ts
GapReport { profileId; schoolId; programId; schoolName; programName; isFictional; targetTerm; asOf;
  hasTransferProgram: Provenance & {value: boolean}; gpa: { student; min: GpaBenchmark; competitive: GpaBenchmark; status: GpaStatus };
  units: Provenance & { requiredMin: number|null; requiredSystem; studentSystem; studentCompleted; studentInProgress; converted; completedInRequiredSystem; projectedInRequiredSystem; shortfall: number|null; status: UnitsStatus };
  prereqs: (Provenance & { courseId; name; status: 'met'|'missing'|'unknown-equivalency'; matchedCourses: string[]; pendingCompletion; possibleEquivalents: string[]; plannedCourses: string[] })[];
  gePattern: Provenance & {text: string|null}; essays: { required: EssayFinding[]; optional: EssayFinding[]; requiredCount; optionalCount };
  recs: Provenance & {required: number}; aiPolicy: Provenance & {mode: string};
  deadline: (DeadlineItem & { status: 'upcoming'|'passed'; termMatched }) | null; allDeadlines: DeadlineItem[];
  warnings: (Provenance & { code: WarningCode; severity: 'blocker'|'warning'|'info'; message; schoolId; programId; relatedRequirementIds })[];
  summary: { met; missing; unknownEquivalency; blockers; warnings; info } }
Provenance { requirementId; sourceUrl; confidence }   // NOTE: no retrieved_at, which is why lookupSource exists
GpaBenchmark = Provenance & { value: number|null; published: boolean }
EssayFinding = Provenance & { essayId; prompt: string|null; promptPublished; wordLimit: number|null; charLimit: number|null; required }
DeadlineItem = Provenance & { deadlineId; term; type: 'filing_open'|'application'|'docs'; date: 'YYYY-MM-DD'; daysUntil: int }
GpaStatus = 'at_or_above_competitive'|'meets_minimum'|'below_competitive'|'below_minimum'|'not_published'
UnitsStatus = 'met'|'met_with_in_progress'|'short'|'not_published'
```
Data files: `web/src/lib/core/data/schools.json` (dataset_version '2026-09-13', cycle 'Fall 2027', disclaimer, 5 schools, the fictional one is `is_fictional: true`), `web/src/lib/core/data/profiles/demo.json` (semester), `web/src/lib/core/data/profiles/demo-quarter.json`.
requirementId format: `${program_id}#${field}` for scalars (has_transfer_program, min_gpa, competitive_gpa, units, ge_pattern, recs_required, ai_policy), `#course:${id}`, `#deadline:${id}`, `#essay:${id}`.
From web/src/routes/api/health/+server.ts (prerender=false): `json({ ok: true, service: 'transferpilot', target: process.env.VERCEL ? 'vercel' : 'local', time })`.
Recording (Phase 4, 04-03): `web/static/data/hero-run.json` = `{ recordedAt: ISO, commitSha, profileId, plan: Plan, preflight?: Record<actionId,'new'|'exists'>, events: TraceEvent[], report: RunReport, rerun?: { events, report } }`, PII redacted. It is regenerated with `npm --prefix web run record`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure lib/ui modules (config, status, format, dataset, provenance, profile-form, data, mode) with unit tests</name>
  <files>web/src/lib/ui/config.ts, web/src/lib/ui/status.ts, web/src/lib/ui/format.ts, web/src/lib/ui/dataset.ts, web/src/lib/ui/provenance.ts, web/src/lib/ui/profile-form.ts, web/src/lib/ui/data.ts, web/src/lib/ui/mode.ts, web/src/lib/ui/foundation.test.ts</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: sections "Status Semantics", "Mode detection", "Static recording files", "SourceLink.svelte", "Step 1: Profile panel", "Copywriting Contract"
    - web/src/lib/core/schemas.ts, web/src/lib/core/schemas/school.ts, web/src/lib/core/schemas/profile.ts, web/src/lib/core/gap/types.ts
    - web/src/lib/core/data/schools.json (top-level keys and one program), web/src/lib/core/data/profiles/demo.json
    - web/static/data/hero-run.json (confirm it exists; if missing run `npm --prefix web run record`, which is Phase 4's recorder, before writing tests)
    - .planning/phases/04-sprint-pipeline-on-mocks/04-03-SUMMARY.md (actual recording shape; if it differs from the <interfaces>, follow the real file)
    - web/vite.config.ts (vitest `requireAssertions: true`, node environment)
  </read_first>
  <behavior>
    status.ts:
    - traceChip('retried', 3) → { tone: 'warn', glyph: '↻', label: 'Retried ×3' }. traceChip('deduped').label === 'Deduped'; traceChip('blocked').tone === 'block'.
    - artifactChip('mismatch') → tone 'fail', glyph '≠', label 'Mismatch: reported OK, not found on read-back'. artifactChip('skipped').label === 'Not approved'.
    - prereqChip({ status: 'met', pendingCompletion: true }).label === 'Met (in progress)'; prereqChip({ status: 'unknown-equivalency', pendingCompletion: false }) → warn, '?', 'Unknown equivalency'.
    - deadlineChip:
      - {daysUntil: 45, status:'upcoming'} → idle '45 days left'
      - 12 → warn '12 days left'
      - 0 → warn 'Due today'
      - {daysUntil: -3, status:'passed'} → fail 'Passed 3 days ago'
      - null → idle 'No deadline published'
    - unitsChip({ status: 'short', shortfall: 6 }).label === 'Short by 6'.
    - gpaChip('below_minimum').tone === 'fail'.
    - confidenceChip('LOW') → warn 'Low confidence: verify'; confidenceChip('HIGH') → idle 'High confidence'.
    - rateTone: 0.9 → 'ok', 0.89 → 'warn', 0.5 → 'warn', 0.49 → 'fail'.
    - runStatusChip('partial').label === 'Some artifacts failed verification. See the failed items below.'
    - Every TONE_CLASSES[tone] contains the literal `bg-${tone}-bg`, `text-${tone}-fg` and `border-${tone}-border`. The test may build the expected strings; the source may not.
    format.ts:
    - fmtDate('2026-11-30') === 'Nov 30, 2026' and fmtDate('2026-01-01') === 'Jan 1, 2026' (no timezone shift)
    - fmtDateTime('2026-09-13T20:02:00.000Z', 'UTC') === 'Sep 13, 2026, 8:02 PM' (U+202F normalized to a plain space)
    - fmtPct(0.956) === '96%'; fmtMs(12.4) === '12 ms'; sha7('abcdef1234567') === 'abcdef1'
    - truncate('x'.repeat(200), 160) has length 160 and ends with '…'
    - localToday() matches /^\d{4}-\d{2}-\d{2}$/
    provenance.ts:
    - For EVERY program in DATASET and EVERY id in requirementIds(program): lookupSource(DATASET, id) is non-null, `retrieved_at` matches /^\d{4}-\d{2}-\d{2}$/, and confidence ∈ HIGH|MEDIUM|LOW.
    - Every requirement of the fictional school's programs has source_url 'about:fictional' (or at least one does, per dataset).
    - lookupSource(DATASET, 'nope#min_gpa') === null; lookupSource(DATASET, `${pid}#course:does-not-exist`) === null; lookupSource(DATASET, 'garbage') === null.
    - parseRequirementId('p#course:calc-1') → { programId: 'p', kind: 'course', id: 'calc-1' }; parseRequirementId('p#min_gpa') → { programId: 'p', kind: 'scalar', id: 'min_gpa' }.
    profile-form.ts:
    - parseProfile(JSON.stringify(demo, null, 2)) → { ok: true, profile.profile_id: 'demo' }.
    - parseProfile('{') → ok false, jsonError.line >= 1, jsonError.column >= 1, and a non-empty message.
    - A profile with `"gpa": 5` → ok false, and issues has an entry with path 'gpa'.
    - setField(text, 'gpa', 3.9) → reparsed gpa 3.9. setField(text, 'units.system', 'quarter') → reparsed units.system 'quarter'. setField(text, 'gpa', NaN) stores null, which yields an issue at path 'gpa'.
    - toggleTarget(text, program, DATASET): adds { school_id, program_id, term: DATASET.cycle } when absent and removes it when present. Toggling twice returns the original targets.
    - fieldIssues(issues, 'units.completed') returns only that path's issues. unmappedIssues(issues) excludes the FIELD_PATHS paths (intended_major, gpa, units.completed, units.in_progress, units.system, terms_remaining, targets, and any `targets.N...` path).
    - targetOptions(DATASET).length === the total program count; the fictional school's options have isFictional true.
    data.ts:
    - loadStatic('/x', fromSchema(z.object({a: z.number()})), fakeFetch):
      - 200 + {a:1} → { state: 'ok', data: {a:1} }
      - 404 → { state: 'error', status: 404, reason: 'http' }
      - 200 + {a:'x'} → reason 'invalid' with a message naming the path
      - fetch throws → reason 'network', status null
    - fromSchema(HeroRunFile) parses `web/static/data/hero-run.json` (read with node:fs in the test) with state-equivalent ok.
    mode.ts:
    - detectMode:
      - url with `?replay` → 'replay' and fetchFn is never called
      - liveAvailable false → 'replay' and fetchFn is never called
      - fetch 404 text/html → 'replay'
      - 200 application/json {target:'local'} → 'replay'
      - 200 application/json; charset=utf-8 {target:'vercel'} → 'live'
      - fetch that never resolves until aborted, with timeoutMs 10 → 'replay'
      - fetch throws → 'replay'
    - fetchFn is called with `${base}/api/health` and no trailing slash.
  </behavior>
  <action>
RED: write `web/src/lib/ui/foundation.test.ts` covering every <behavior> bullet, grouped in `describe` blocks per module. Import the demo profile and schools JSON through relative paths (`../core/data/...`) and read hero-run.json with `node:fs` resolved from `import.meta.url`. Run the tests and confirm they fail. Then GREEN:

1. `config.ts`:
   - `LIVE_URL = ''` and `LIVE_SOURCE_AVAILABLE = false` (Phase 9 sets it true when LiveSource exists; until then the probe result is not used to switch sources)
   - `REPO_URL = 'https://github.com/wolfwdavid/multi-app-agent'`; `BRIEF_URL = `${REPO_URL}/blob/main/BRIEF.md``
   - `DATA_PATHS = { hero: 'data/hero-run.json', evals: 'data/evals.json', silentFailure: 'data/silent-failure-run.json' } as const`
   - `APP_LABELS: Record<AppName, string>`: notion 'Notion', calendar 'Google Calendar', docs 'Google Docs', gmail 'Gmail', github 'GitHub', hf 'Hugging Face'
   - `PLAN_APP_ORDER = ['notion','calendar','docs','gmail'] as const`
   - `REPLAY_SPEEDS = [{ id: '1x', label: '1×', ms: 400 }, { id: '4x', label: '4×', ms: 100 }] as const`
2. `status.ts`: `type Tone = 'ok'|'warn'|'info'|'block'|'fail'|'idle'`; `interface ChipSpec { tone: Tone; glyph: string; label: string }`; `type DisplayStatus = 'queued'|'running'|'ok'|'retried'|'deduped'|'blocked'|'failed'`.
   - `TONE_CLASSES: Record<Tone, string>` is six literal strings: `'bg-ok-bg text-ok-fg border-ok-border'`, `'bg-warn-bg text-warn-fg border-warn-border'`, `'bg-info-bg text-info-fg border-info-border'`, `'bg-block-bg text-block-fg border-block-border'`, `'bg-fail-bg text-fail-fg border-fail-border'`, `'bg-idle-bg text-idle-fg border-idle-border'`.
   - `TONE_OUTLINE: Record<Tone, string>`, also literal: `'bg-transparent text-ok-fg border-ok-border'` and so on for each tone.
   - Implement traceChip, artifactChip, prereqChip, severityChip, gpaChip, unitsChip, deadlineChip, `fictionalChip()` (block '◆' 'Fictional test school'), confidenceChip, `rateTone`, `runStatusChip` (UI-SPEC "Run status banner" copy), `countTone(n, tone) => n === 0 ? 'idle' : tone`, and `AI_POLICY_LABELS`. Glyphs and labels are verbatim from UI-SPEC "Status Semantics".
3. `format.ts`:
   - `fmtDate(ymd)` uses `Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric', timeZone: 'UTC' })` on `new Date(ymd + 'T00:00:00Z')`.
   - `fmtDateTime(iso, timeZone?)` uses month short, day numeric, year numeric, hour numeric, minute 2-digit, then `.replace(/[\u202f\u00a0]/g, ' ')`.
   - Also: `fmtPct(x) = Math.round(x * 100) + '%'`, `fmtMs(ms) = Math.round(ms) + ' ms'`, `sha7`, `truncate(s, n)` (slice to n-1 chars plus '…' when longer), and `localToday() = new Date().toLocaleDateString('en-CA')`.
4. `dataset.ts`: `import schoolsJson from '../core/data/schools.json'`, plus `demo` and `demo-quarter` from `../core/data/profiles/`.
   - `export const DATASET = SchoolsDataset.parse(schoolsJson)`
   - `export const DEMO_PROFILES = [{ id: 'demo', label: 'Demo student (semester units)', profile: Profile.parse(demoJson) }, { id: 'demo-quarter', label: 'Demo student (quarter units)', profile: Profile.parse(demoQuarterJson) }] as const`
5. `provenance.ts`:
   - `parseRequirementId(id)`: split on the FIRST '#', so the kind is 'scalar' | 'course' | 'deadline' | 'essay'; return null when malformed.
   - `lookupSource(ds, id): (SourceRef & {}) | null`: build the program index once and cache it per dataset in a `WeakMap`.
     - scalar → `program[field]` when field ∈ SCALAR_REQUIREMENT_FIELDS
     - course → `required_courses.find(c => c.id === id)`
     - deadline → `deadlines.find`
     - essay → `essays.find`
     - Return `{ source_url, additional_source_urls, retrieved_at, confidence, note }`.
6. `profile-form.ts`:
   - `type ProfileIssue = { path: string; message: string }`
   - `type ParseResult = { ok: true; profile: Profile } | { ok: false; jsonError?: { line: number; column: number; message: string }; issues: ProfileIssue[] }`
   - `parseProfile(text)`: try JSON.parse. On failure, take line/column from the message via `/line (\d+) column (\d+)/`. Otherwise use `/position (\d+)/`, converted by counting '\n' in `text.slice(0, pos)`. Otherwise (e.g. 'Unexpected end of JSON input') use the last line and column. Clean the message with `.replace(/\s*in JSON at position.*$/, '')`. Then run `Profile.safeParse` and map the issues with `path: issue.path.join('.')`.
   - `FIELD_PATHS = ['intended_major','gpa','units.completed','units.in_progress','units.system','terms_remaining','targets'] as const`
   - `fieldIssues(issues, path)`: `targets` also matches `targets.*`.
   - `unmappedIssues(issues)`
   - `setField(text, path, value)`: JSON.parse (the caller only calls it when the text is valid JSON), set the dotted path, write NaN as null, return `JSON.stringify(obj, null, 2)`.
   - `toggleTarget(text, { schoolId, programId }, ds)`
   - `targetOptions(ds): { schoolId; programId; label: `${school.name} — ${program.name}`; isFictional }[]`
7. `data.ts`:
   - `type LoadResult<T> = { state: 'ok'; data: T } | { state: 'error'; status: number | null; reason: 'http' | 'invalid' | 'network'; message: string }`
   - `type Parser<T> = (raw: unknown) => { ok: true; data: T } | { ok: false; message: string }`
   - `fromSchema(schema)`: safeParse, joining the first 3 issues as `path: message`.
   - `loadStatic<T>(url, parse, fetchFn = fetch)`: never throws, and never returns partial data.
   - `export const HeroRunFile = z.object({ recordedAt: z.string().min(1), commitSha: z.string().min(1), profileId: z.string().min(1), plan: Plan, preflight: z.record(z.string(), z.enum(['new','exists'])).optional(), events: z.array(TraceEvent), report: RunReport, rerun: z.object({ events: z.array(TraceEvent), report: RunReport }).optional() })` plus `export type HeroRunFile`. Build it from `../core/schemas.ts` rather than importing `core/agent/recording.ts`, so the client bundle never pulls in executor or connector code. Add a comment saying it mirrors 04-03 `HeroRunFile`.
8. `mode.ts`: `export type AppMode = 'detecting' | 'live' | 'replay'`.
   - `export async function detectMode({ url, base, liveAvailable, fetchFn = fetch, timeoutMs = 1500 }): Promise<'live' | 'replay'>`, per UI-SPEC "Mode detection".
   - An `AbortController` with setTimeout provides the timeout; clear the timer in `finally`.
   - `live` only when `res.ok`, content-type includes 'application/json', and `body.target === 'vercel'`.
   - Everything else → 'replay'. Wrap the whole probe in try/catch.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test -- src/lib/ui/foundation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with >= 30 passing assertions spread over >= 8 `describe` blocks
    - `grep -rnE "from '\\$app|from '\\$env|from 'svelte" web/src/lib/ui/*.ts | grep -v "mode.svelte.ts"` prints nothing
    - `grep -c "bg-ok-bg text-ok-fg border-ok-border" web/src/lib/ui/status.ts` >= 1 and `grep -cE '\$\{tone\}' web/src/lib/ui/status.ts` == 0
    - `grep -c "export const HeroRunFile" web/src/lib/ui/data.ts` == 1 and `grep -c "core/agent" web/src/lib/ui/data.ts` == 0
    - `grep -c "api/health" web/src/lib/ui/mode.ts` == 1 and `grep -c "api/health/" web/src/lib/ui/mode.ts` == 0
    - `git diff --name-only -- web/src/lib/core web/package.json` prints nothing
  </acceptance_criteria>
  <done>Status, format, provenance, profile parsing, static loading and mode detection are pure, tested modules that components consume. Every requirement id in the dataset resolves to a source with a retrieved date and confidence.</done>
</task>

<task type="auto">
  <name>Task 2: Tokens + global shell (layout.css, +layout, header, mode state, replay banner) + shared chips/badge/SourceLink/EmptyState + evals route shell</name>
  <files>web/src/routes/layout.css, web/src/routes/+layout.ts, web/src/routes/+layout.svelte, web/src/lib/ui/mode.svelte.ts, web/src/lib/components/AppHeader.svelte, web/src/lib/components/ModeBanner.svelte, web/src/lib/components/StatusChip.svelte, web/src/lib/components/ConfidenceBadge.svelte, web/src/lib/components/SourceLink.svelte, web/src/lib/components/EmptyState.svelte, web/src/routes/evals/+page.svelte</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: "Non-negotiable platform rules", "Spacing Scale / Layout grid", "Typography", "Color" (the exact layout.css block), "Global shell", "Mode detection", "ModeBanner", "SourceLink.svelte", "Component Inventory", "Accessibility Contract"
    - web/src/routes/+layout.svelte, web/src/routes/+layout.ts, web/src/routes/layout.css (current scaffold)
    - web/src/lib/ui/config.ts, status.ts, format.ts, dataset.ts, provenance.ts, data.ts, mode.ts (Task 1)
  </read_first>
  <action>
1. `web/src/routes/layout.css`: replace the whole file with the exact CSS block from UI-SPEC "Exact web/src/routes/layout.css".
2. `web/src/routes/+layout.ts`: `export const prerender = true;` and `export const trailingSlash = 'always';`.
3. `web/src/lib/ui/mode.svelte.ts`: `export const appState = $state<{ mode: AppMode; hero: LoadResult<HeroRunFile> | null }>({ mode: 'detecting', hero: null })`.
   - `let heroPromise: Promise<LoadResult<HeroRunFile>> | null = null; export function loadHero(base: string)`: memoized `loadStatic(`${base}/${DATA_PATHS.hero}`, fromSchema(HeroRunFile))`, and assigns `appState.hero` when it resolves.
   - `let started = false; export async function initApp(url: URL, base: string)`: idempotent. `appState.mode = await detectMode({ url, base, liveAvailable: LIVE_SOURCE_AVAILABLE })`; when the mode is 'replay', `await loadHero(base)`.
   - Import from `./mode.ts`, `./data.ts` and `./config.ts` (relative).
4. `web/src/routes/+layout.svelte`:
   - Keep `import './layout.css'` and the favicon.
   - `onMount(() => { initApp(page.url, base); })`.
   - Markup, in order: skip link `<a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 rounded-md bg-surface px-4 py-2">Skip to content</a>`; `<AppHeader mode={appState.mode} />`; `{#if appState.mode === 'replay'}<ModeBanner hero={appState.hero} />{/if}`; `<main id="main" tabindex="-1" class="mx-auto w-full max-w-6xl px-4 sm:px-6 pb-16">{@render children()}</main>`.
5. `AppHeader.svelte` (per UI-SPEC "Global shell"):
   - `<header class="bg-surface border-b border-border">` containing an inner `mx-auto max-w-6xl px-4 sm:px-6 h-14 flex items-center justify-between gap-4`.
   - Wordmark "TransferPilot" → `{base}/`.
   - `<nav aria-label="Primary">` links: Sprint `{base}/`, Evals `{base}/evals/`, and GitHub ↗ (REPO_URL, target _blank, rel noopener noreferrer, sr-only "(opens in new tab)").
   - Every link is `min-h-11 inline-flex items-center`.
   - Active state comes from `page.route.id`: '/' → Sprint, startsWith('/evals') → Evals. This is base-independent, so prerendered relative paths stay correct. Active links get `aria-current="page"` and `border-b-2 border-accent`.
   - Mode pill: detecting → `StatusChip chip={{tone:'idle', glyph:'…', label:'Checking…'}}`; replay → info '▶' 'Recorded run'; live → ok '●' 'Live · mock apps'.
6. `ModeBanner.svelte` (prop `hero: LoadResult<HeroRunFile> | null`):
   - `<div class="bg-info-bg text-info-fg border-b border-info-border"><p class="mx-auto max-w-6xl px-4 sm:px-6 py-2 text-sm">`.
   - Text: **Recorded run.** + UI-SPEC banner copy, with `fmtDate(hero.data.recordedAt.slice(0,10))` and `<code class="font-mono">{sha7(commitSha)}</code>`.
   - If hero is null or in an error state, render the same sentence without the date/commit clause ("This static site replays a recorded mock-mode run.").
   - Append "Open the live version ↗" only when `LIVE_URL` is set.
7. `StatusChip.svelte`: props `{ chip }: { chip: ChipSpec }`. Renders `<span class="inline-flex items-center gap-1 rounded-md border px-2 py-1 text-sm font-semibold {TONE_CLASSES[chip.tone]}"><span aria-hidden="true">{chip.glyph}</span>{chip.label}</span>`.
8. `ConfidenceBadge.svelte`: props `{ confidence, note? }`. Outline badge `inline-flex items-center rounded-md border px-2 py-1 text-sm font-semibold {TONE_OUTLINE[tone]}` with `title={note}` and the label from `confidenceChip`.
9. `SourceLink.svelte`: props `{ requirementId, variant = 'full', label = 'this requirement' }`. `src = $derived(lookupSource(DATASET, requirementId))`. Implement exactly per UI-SPEC "SourceLink.svelte":
   - full → "Verify on official page ↗" with sr-only "(opens in new tab)"
   - compact → "Verify ↗" with `aria-label="Verify {label} on the official page (opens in new tab)"`
   - Both variants → `· Retrieved {fmtDate(retrieved_at)}` plus ConfidenceBadge
   - 'about:fictional' → the muted "Fictional school: no official page" plus the badge
   - `note` → the badge title, plus `<details><summary>Source note</summary>` in the full variant only
   - null lookup → muted "Source not found" (defensive; the Task 1 tests guarantee it never happens for dataset ids)
10. `EmptyState.svelte`: props `{ heading, body, tone = 'idle', children? }` (children is a Snippet). Markup `<div class="rounded-lg border border-dashed border-border p-6 text-center space-y-2" class:border-fail-border={tone==='fail'}>`, then an `<p class="text-base font-semibold">` heading, the body in `text-sm text-fg-muted`, and `{@render children?.()}`.
11. `web/src/routes/evals/+page.svelte` (shell; 07-02 fills in the dashboard). It must exist now because the header links to `{base}/evals/` and the prerender crawler fails the build on a 404:
    - `<svelte:head><title>Evals · TransferPilot</title></svelte:head>`
    - H1 "Eval results" (`text-2xl font-semibold`) with `pt-8`, and the UI-SPEC subtitle
    - the "Read the reliability brief (BRIEF.md) ↗" link (BRIEF_URL)
    - `<p class="text-sm text-fg-muted" role="status">Loading eval results…</p>`
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web run check && MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build && test -f web/build/evals/index.html && test -f web/build/index.html && test -f web/build/.nojekyll && grep -q "Eval results" web/build/evals/index.html && grep -q "Skip to content" web/build/index.html && grep -q "/multi-app-agent/_app" web/build/404.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run check` exits 0 with 0 errors
    - The static build with `BASE_PATH=/multi-app-agent` exits 0 and emits `web/build/index.html`, `web/build/evals/index.html` and `web/build/.nojekyll`
    - `grep -c "trailingSlash = 'always'" web/src/routes/+layout.ts` == 1
    - `grep -c "\-\-color-block-border: var(--tp-block-border)" web/src/routes/layout.css` == 1 and `grep -c "prefers-color-scheme: dark" web/src/routes/layout.css` == 1
    - `grep -rnE "href=\"/|href='/|fetch\(\s*[\"'\`]/" web/src/routes web/src/lib/components --include=*.svelte` prints nothing (no root-absolute internal URLs)
    - `grep -c "aria-current" web/src/lib/components/AppHeader.svelte` >= 1 and `grep -c "page.route.id" web/src/lib/components/AppHeader.svelte` >= 1
    - `grep -c "Verify on official page" web/src/lib/components/SourceLink.svelte` == 1 and `grep -c "Retrieved" web/src/lib/components/SourceLink.svelte` >= 1 and `grep -c "about:fictional" web/src/lib/components/SourceLink.svelte` == 1
    - `grep -c "Recorded run" web/src/lib/components/ModeBanner.svelte web/src/lib/components/AppHeader.svelte` totals >= 2
  </acceptance_criteria>
  <done>The site has the approved tokens, an accessible header with base-safe navigation, replay-vs-live detection with the recorded-run banner, and reusable chip, badge, source-link and empty-state components. The evals route exists, so prerender crawling succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Sprint steps 1–2: ProfilePanel (picker, fields, JSON editor, inline zod errors) + GapReportCard + sprint page layout with step placeholders</name>
  <files>web/src/lib/components/ProfilePanel.svelte, web/src/lib/components/GapReportCard.svelte, web/src/routes/+page.svelte</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: "Screen 1: Sprint page" (header block, "Step 1: Profile panel", "Step 2: Gap report", "GapReportCard.svelte" rows table), "Layout grid", "Copywriting Contract", "Accessibility Contract" items 1, 5, 6, "Build Priority"
    - web/src/lib/ui/profile-form.ts, status.ts, format.ts, dataset.ts (Task 1)
    - web/src/lib/components/StatusChip.svelte, SourceLink.svelte, EmptyState.svelte (Task 2)
    - web/src/lib/core/gap/types.ts (GapReport fields)
  </read_first>
  <action>
1. `ProfilePanel.svelte`:
   - Props: `let { profile = $bindable<Profile | null>(null), valid = $bindable(true), pristine = $bindable(true), targetsEmpty = $bindable(false) } = $props()`.
   - State: `source = $state<'demo'|'demo-quarter'>('demo')`; `pristineText = $state(JSON.stringify(DEMO_PROFILES[0].profile, null, 2))`; `profileText = $state(pristineText)`; `parsed = $derived(parseProfile(profileText))`.
   - `$effect`:
     - when parsed.ok → `profile = parsed.profile; valid = true`
     - otherwise → `valid = false` (the last valid profile stays)
     - always → `pristine = profileText === pristineText && source === 'demo'` and `targetsEmpty = !parsed.ok && parsed.issues.length > 0 && parsed.issues.every(i => i.path === 'targets' || i.path.startsWith('targets'))`
   - Markup, top to bottom, per UI-SPEC "Step 1: Profile panel":
     a. Summary: when `!parsed.ok`, a `role="status"` summary. With a jsonError: "Invalid JSON at line {line}, column {column}: {message}". Otherwise: "Profile has {n} error(s). Fix the highlighted fields to update the gap report." followed by `<ul id="pf-unmapped">` with `path: message` from `unmappedIssues`. Text is `text-sm text-danger`.
     b. `<fieldset>` with legend "Start from": two radio cards (`grid grid-cols-2 gap-2`, each label `min-h-11 rounded-lg border border-border p-2`, radio `text-accent`). On change while `profileText !== pristineText`, ask `confirm(`Discard your profile edits and load the ${label} profile?`)`. Cancel restores the radio selection; OK loads that profile's pretty JSON into both `pristineText` and `profileText`.
     c. Fields (`space-y-4`), bound to the current object via `parsed.ok ? parsed.profile : lastValid`. Each input's `oninput` writes `profileText = setField(profileText, path, value)` and is disabled while `parsed.jsonError` exists (the JSON must be fixed first).
        - "Intended major" (text, id pf-major)
        - "GPA (0–4)" (number step 0.01 inputmode decimal, id pf-gpa)
        - "Units completed" / "Units in progress" (`grid grid-cols-2 gap-2`)
        - "Unit system" (select Semester/Quarter)
        - "Terms remaining before transfer" (number step 1)
        - "Target programs": checkbox list from `targetOptions(DATASET)`, `size-5 text-accent` inside `min-h-11` label rows, with the fictional chip on fictional options; toggling calls `toggleTarget`
        - Each field with `fieldIssues(...)` gets `aria-invalid="true"`, `aria-describedby="{id}-error"`, the `border-danger` class, and `<p id="{id}-error" class="text-sm text-danger">` below it.
     d. `<details>` "Advanced: edit the full profile JSON": `<textarea bind:value={profileText} spellcheck="false" rows="16" class="font-mono text-sm w-full rounded-md" aria-describedby="pf-json-help pf-unmapped">`, plus helper `<p id="pf-json-help">` "Validated against the Profile schema as you type."
     e. Footer (`text-sm text-fg-muted`): "Demo data only. Edits stay in this browser tab."
2. `GapReportCard.svelte`: prop `{ report }: { report: GapReport }`. Implement per UI-SPEC "GapReportCard.svelte" items 1–6 and the rows table:
   - `<article class="rounded-lg border border-border bg-surface p-4 sm:p-6 space-y-4">`
   - Header: h3 schoolName; programName · targetTerm; deadlineChip plus `fmtDate(report.deadline.date)`; fictionalChip when `isFictional`.
   - Summary chips use `countTone`.
   - Warnings: `<ul class="space-y-2">`, each `<li>` a `rounded-md border p-2` with `TONE_CLASSES[severityChip(w.severity).tone]`. Line 1: severity chip + message. Line 2: `font-mono` code · `<SourceLink requirementId={w.requirementId} variant="compact" label={w.code} />`.
   - Requirements: a `<dl class="divide-y divide-border">` with rows GPA, Units (converted and non-converted copy), Transfer program, Recommendations, Essays (`<details>` list with prompt or "Prompt not publicly available; check the application portal.", limit text, and a SourceLink per essay), GE pattern, AI policy (AI_POLICY_LABELS). **Every row has its own SourceLink (full variant)** on its own line. GPA also shows the competitive link when `gpa.competitive.requirementId !== gpa.min.requirementId`.
   - Prerequisites: "Required courses" list with prereqChip, name, evidence copy per UI-SPEC, and a compact SourceLink with `label={p.name}`.
   - `<details>` "All deadlines ({n})": type label (Filing opens / Application due / Documents due) · fmtDate · "{daysUntil} days".
   - Compressible items (cut only if over the time box, in UI-SPEC order): All deadlines, then the essay details list, GE and AI-policy rows. Never cut the GPA/units/prereq/warning SourceLinks.
3. `web/src/routes/+page.svelte`: replace the placeholder entirely.
   - `<svelte:head><title>Sprint · TransferPilot</title></svelte:head>`
   - State: `let profile = $state<Profile | null>(null)`, `let valid = $state(true)`, `let pristine = $state(true)`, `let targetsEmpty = $state(false)`, `const today = localToday()`, `const reports = $derived(profile ? analyzeGaps(profile, DATASET, { today }) : [])`.
   - `profile` starts null, so to prerender the demo report initialise it from `DEMO_PROFILES[0].profile`. The prerendered HTML then contains the gap report, and hydration recomputes it with the visitor's date.
   - Header block (`pt-8 space-y-2`): H1 "Application sprint", the UI-SPEC subtitle, and `<nav aria-label="Sprint steps">` with hash links `#profile #gaps #plan #trace #results` labelled "1 Profile · 2 Gaps · 3 Plan · 4 Run · 5 Verified" (`text-sm`). A step shows '✓' when its data exists; in this plan that means profile valid → 1 and reports.length > 0 → 2.
   - Grid `mt-8 grid grid-cols-1 lg:grid-cols-12 gap-6`:
     - Aside `lg:col-span-4 lg:sticky lg:top-4 lg:self-start lg:max-h-[calc(100vh-2rem)] lg:overflow-y-auto`: `<section id="profile"><details open class="rounded-lg border border-border bg-surface p-4 sm:p-6"><summary class="min-h-11 cursor-pointer"><h2 class="inline text-xl font-semibold">1. Profile</h2></summary><ProfilePanel bind:profile bind:valid bind:pristine bind:targetsEmpty /></details></section>`
     - Main `lg:col-span-8 space-y-12`:
       - `<section id="gaps">`: h2 "2. Gap report" plus the meta line "As of {asOf} · Dataset {DATASET.dataset_version} · Deterministic: no AI involved in these findings." (asOf = `reports[0]?.asOf ?? today`), the disclaimer verbatim, then:
         - `targetsEmpty` → the "No target programs" EmptyState
         - else when `!valid` → the muted "Showing results for the last valid profile." followed by the cards
         - `{#each reports as report (report.programId)}<GapReportCard {report} />{/each}` in `space-y-4`
       - `<section id="plan">`: h2 "3. Plan & approve" + subtitle + EmptyState "No plan yet"
       - `<section id="trace">`: h2 "4. Run trace" + EmptyState "Nothing has run yet"
       - `<section id="results">`: h2 "5. Verified results" + subtitle + EmptyState "No artifacts yet"
       - These three placeholder sections use UI-SPEC copy verbatim; 07-02 replaces their bodies. Export nothing else.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test && npm --prefix web run check && MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build && grep -q "Application sprint" web/build/index.html && grep -q "Verify on official page" web/build/index.html && grep -q "Retrieved" web/build/index.html && grep -q "confidence" web/build/index.html && ! grep -q "Welcome to SvelteKit" web/build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web test` (full suite, core + ui) exits 0; `npm --prefix web run check` exits 0 errors; the static build exits 0
    - Prerendered `web/build/index.html` contains `Application sprint`, `2. Gap report`, `Verify on official page`, `Retrieved`, `Fictional school: no official page`, `Deterministic: no AI involved` and `Demo data only`
    - `grep -o "Verify on official page" web/build/index.html | wc -l` >= 20 (full-variant links on every requirement row across all target cards)
    - `grep -c "aria-invalid" web/src/lib/components/ProfilePanel.svelte` >= 1, `grep -c "aria-describedby" web/src/lib/components/ProfilePanel.svelte` >= 2, `grep -c "Invalid JSON at line" web/src/lib/components/ProfilePanel.svelte` == 1, `grep -c "confirm(" web/src/lib/components/ProfilePanel.svelte` == 1
    - `grep -c "Showing results for the last valid profile." web/src/routes/+page.svelte` == 1 and `grep -c "analyzeGaps(" web/src/routes/+page.svelte` == 1
    - `grep -c "SourceLink" web/src/lib/components/GapReportCard.svelte` >= 8
    - `grep -rnE "font-bold|font-medium" web/src/lib/components web/src/routes --include=*.svelte` prints nothing (2 weights only)
  </acceptance_criteria>
  <done>A visitor can load either demo profile or edit fields and JSON, and sees inline schema errors. The deterministic gap report updates live in the browser, with a verify link, retrieved date and confidence badge next to every requirement.</done>
</task>

</tasks>

<verification>
Run sequentially from Git Bash in the repo root:
1. `npm --prefix web test`: all suites green, including `src/lib/ui/foundation.test.ts` and core `boundary.test.ts`.
2. `npm --prefix web run check`: 0 errors.
3. `MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build`: exits 0, emits `index.html`, `evals/index.html`, `404.html` and `.nojekyll`.
4. `git diff --name-only -- web/src/lib/core web/src/routes/api web/package.json` prints nothing.
5. `grep -rnE "href=\"/|fetch\(\s*[\"'\`]/" web/src/routes web/src/lib/components web/src/lib/ui` prints nothing.
</verification>

<success_criteria>
- ROADMAP Phase 7 criterion 1: the user loads the seeded demo profile or edits the profile form, schema errors show inline, and the gap report shows a "verify on official page" link and retrieved date next to every requirement.
- DATA-03: the profile covers the major, GPA, units + system, terms and target programs as fields, and every profile field through the JSON editor, all validated by the Profile zod schema.
- The shell satisfies the UI-SPEC platform rules (base prefix, trailingSlash, .nojekyll, core boundary, injected today, UTC date formatting) and shows replay mode on static hosts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-01-SUMMARY.md`. Include:
- the exported signatures of every `lib/ui` module (07-02 imports them)
- component props
- any `base` vs `resolve()` deviation
- any hero-run.json shape difference found
</output>
