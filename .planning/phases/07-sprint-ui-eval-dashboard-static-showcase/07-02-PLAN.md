---
phase: 07-sprint-ui-eval-dashboard-static-showcase
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/src/lib/ui/trace-rows.ts
  - web/src/lib/ui/pacer.ts
  - web/src/lib/ui/sprint-source.ts
  - web/src/lib/ui/evals.ts
  - web/src/lib/ui/fixtures.ts
  - web/src/lib/ui/trace-rows.test.ts
  - web/src/lib/ui/sprint-source.test.ts
  - web/src/lib/ui/evals.test.ts
  - web/src/lib/components/TraceTimeline.svelte
  - web/src/lib/components/ReplayControls.svelte
  - web/src/lib/components/PlanApproval.svelte
  - web/src/lib/components/ActionCard.svelte
  - web/src/lib/components/ArtifactsPanel.svelte
  - web/src/lib/components/StatCard.svelte
  - web/src/lib/components/ScenarioTable.svelte
  - web/src/lib/components/TaxonomyBreakdown.svelte
  - web/src/lib/components/SilentFailureReplay.svelte
  - web/src/routes/+page.svelte
  - web/src/routes/evals/+page.svelte
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "In replay mode, 'Build plan' loads the recorded plan into human-readable action cards grouped Notion → Google Calendar → Google Docs → Gmail. Each card shows app, operation, target, idempotency key, a 'New' / 'Exists: will dedupe' badge and a payload preview. Blockers and injection flags render as callouts"
    - "The user can approve all (master + per-group tri-state checkboxes) or individual actions. 'Run {n} approved actions' is disabled at 0. Unapproved actions produce no trace rows and appear as 'Not approved' in the results"
    - "Running plays the recorded trace one row every 400 ms (Pause/Play, Step, Speed, Restart). Rows show step, app, operation, attempt, latency and a text-labelled status chip (OK, Retried ×n, Deduped, Blocked, Failed), with retry sub-rows. An sr-only polite live region announces each step and the final summary"
    - "The run ends in a verified-artifacts panel: run status banner, verified/deduped/failed/not-approved counts, and artifacts grouped by app. 'Run again' replays the recorded rerun (all deduped) when the recording has one"
    - "/evals/ shows, from data/evals.json: pass-rate, pass^k, scenario and silent-failures-caught stat cards; a per-scenario table with pass rate over N and pass^k per model; all 7 Lemma taxonomy classes in enum order; and a keyboard-steppable replay of the caught silent failure with 'What the tool returned' vs 'What read-back found' and a verdict"
    - "Malformed or missing static JSON never renders partially: loading, empty and error states use UI-SPEC copy, and the BRIEF link stays visible"
  artifacts:
    - path: "web/src/lib/ui/trace-rows.ts"
      provides: "TraceEvent[] → TraceRow[] (UI display-status mapping, attempt sub-rows, readback rows)"
      exports: ["toTraceRows", "isRowTerminal", "countRowSpans", "rowAnnouncement", "runAnnouncement", "oracleStepIndex"]
    - path: "web/src/lib/ui/pacer.ts"
      provides: "play/pause/step/speed/cancel gate for replay pacing"
      exports: ["createPacer", "ReplayCancelled"]
    - path: "web/src/lib/ui/sprint-source.ts"
      provides: "SprintSource interface + ReplaySource over hero-run.json"
      exports: ["SprintSource", "createReplaySource", "filterRecordedRun", "playEvents"]
    - path: "web/src/lib/ui/evals.ts"
      provides: "EvalsFile + SilentFailureRun zod contracts (single adapter point for Phase 6 shape) + dashboard view helpers"
      exports: ["EvalsFile", "SilentFailureRun", "parseEvalsFile", "parseSilentFailureRun", "FAILURE_LABELS", "primaryModel", "llmModel", "taxonomy", "topFailure", "scenarioAllPassed", "adversarialCount"]
    - path: "web/src/routes/evals/+page.svelte"
      provides: "eval dashboard"
    - path: "web/src/lib/components/SilentFailureReplay.svelte"
      provides: "caught silent failure step-through"
  key_links:
    - from: "web/src/routes/+page.svelte"
      to: "web/src/lib/ui/sprint-source.ts"
      via: "createReplaySource(appState.hero.data, { pace: pacer.pace }) → buildPlan / execute / rerun"
      pattern: "createReplaySource\\("
    - from: "web/src/lib/components/TraceTimeline.svelte"
      to: "web/src/lib/ui/status.ts"
      via: "traceChip(row.status, row.attempt) → StatusChip"
      pattern: "traceChip\\("
    - from: "web/src/routes/evals/+page.svelte"
      to: "static/data/evals.json"
      via: "loadStatic(`${base}/${DATA_PATHS.evals}`, parseEvalsFile)"
      pattern: "parseEvalsFile"
    - from: "web/src/lib/components/SilentFailureReplay.svelte"
      to: "web/src/lib/ui/trace-rows.ts"
      via: "toTraceRows(run.events) + oracleStepIndex(rows, run.oracle.stepSpanId)"
      pattern: "oracleStepIndex\\("
---

<objective>
Finish the sprint flow and build the eval dashboard:
1. Pure, tested adapters: trace rows, replay pacing, a ReplaySource over `hero-run.json`, and an evals/silent-failure adapter that is the only place the Phase 6 JSON shape is interpreted.
2. Sprint steps 3–5: plan approval cards, the paced trace timeline, and the verified-artifacts panel (UI-01).
3. The `/evals/` dashboard: stat cards, per-scenario table, taxonomy, and the caught-silent-failure step-through (UI-02).

Purpose: ROADMAP Phase 7 success criteria 2 and 3. Everything runs on committed static JSON, so Pages and HF show real numbers with no backend. Phase 9 only adds a LiveSource behind the same `SprintSource` interface.
Output: 4 `lib/ui` modules + fixtures + 3 test files, 9 components, and 2 routes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-01-SUMMARY.md
@.planning/phases/04-sprint-pipeline-on-mocks/04-03-SUMMARY.md

<conventions>
- 07-UI-SPEC.md is the approved design contract; copy, classes, a11y and layout come from it verbatim. The conventions block of 07-01-PLAN.md applies unchanged:
  - relative `.ts` imports in `lib/ui/*.ts`, and `$lib/...` without an extension in `.svelte`
  - no `$app`/`svelte` imports in pure modules
  - literal Tailwind classes only
  - `base`-prefixed URLs
  - `MSYS_NO_PATHCONV=1` for the Pages build
  - no edits to `web/src/lib/core/**`, `web/src/routes/api/**` or `web/package.json`
  - no new dependencies
- **Phase 6 data shape:** Phase 6 plans were not on disk when this plan was written. Read `.planning/phases/06-*/06-0*-SUMMARY.md` and the actual `web/static/data/evals.json` and silent-failure trace first.
  - If field names differ from the UI-SPEC `EvalsFile` / `SilentFailureRun` shapes, write the translation ONLY inside `web/src/lib/ui/evals.ts` (`parseEvalsFile` / `parseSilentFailureRun` normalize raw input into the UI-SPEC view types) and record the mapping in the SUMMARY. Components consume the normalized types only.
  - **Never fabricate eval numbers.** If `evals.json` is missing, stop and report a blocker; Phase 6 must run `cd web && npx tsx scripts/eval.ts --n 10` (there is no `npm run eval` script).
  - If only the silent-failure trace is missing, the dashboard shows the UI-SPEC "Replay not available" empty state; record it as a gap in the SUMMARY.
- Phase 4 recording: 04-03 produces `web/static/data/hero-run.json`. `HeroRunFile` lives in `web/src/lib/ui/data.ts` (07-01). If the real trace attribute names differ from the list below (see the 04-03 SUMMARY), adapt only `trace-rows.ts`.
- UI-SPEC copy change: the "Recording missing" body says "Run npm run record and rebuild." instead of `scripts/record.ts`, because 04-03 named the recorder `scripts/sprint.ts` with an `npm run record` alias.
- COMMITS: commit messages must NOT contain `Co-Authored-By`, `Claude`, `Claude-Session`, `Generated with`, or any AI-assistant mention. After each commit, `git log -1 --format=%B | grep -inE "co-authored|claude|anthropic"` must print nothing; otherwise amend.
</conventions>

<interfaces>
From 07-01 (confirm against 07-01-SUMMARY.md):
```ts
// lib/ui/status.ts
type Tone = 'ok'|'warn'|'info'|'block'|'fail'|'idle'; interface ChipSpec { tone; glyph; label }
type DisplayStatus = 'queued'|'running'|'ok'|'retried'|'deduped'|'blocked'|'failed';
TONE_CLASSES, TONE_OUTLINE, traceChip(s: DisplayStatus, attempts?), artifactChip(ArtifactStatus), runStatusChip(status), rateTone(rate), countTone(n, tone)
// lib/ui/format.ts: fmtDate, fmtDateTime, fmtPct, fmtMs, sha7, truncate, localToday
// lib/ui/config.ts: LIVE_URL, LIVE_SOURCE_AVAILABLE, REPO_URL, BRIEF_URL, DATA_PATHS {hero, evals, silentFailure}, APP_LABELS, PLAN_APP_ORDER, REPLAY_SPEEDS
// lib/ui/data.ts: LoadResult<T>, Parser<T> = (raw) => {ok:true,data}|{ok:false,message}, fromSchema(schema), loadStatic(url, parse, fetchFn?), HeroRunFile (zod)
// lib/ui/mode.svelte.ts: appState { mode: 'detecting'|'live'|'replay'; hero: LoadResult<HeroRunFile> | null }, initApp(url, base), loadHero(base)
// components: StatusChip { chip }, ConfidenceBadge { confidence, note? }, SourceLink { requirementId, variant, label }, EmptyState { heading, body, tone?, children? }, ProfilePanel (bind: profile, valid, pristine, targetsEmpty), GapReportCard { report }
// routes/+page.svelte already has sections #profile #gaps #plan #trace #results; #plan/#trace/#results hold placeholder EmptyStates that this plan replaces
// routes/evals/+page.svelte holds the H1/subtitle/BRIEF link/"Loading eval results…" shell
```
Phase 1 contracts (web/src/lib/core/schemas.ts): `Plan { planId, profileId, createdAt, actions: Action[], blockers: Blocker[], flags: ContentFlag[] }`, `Action { id, app, tool, effect: 'read'|'write', schoolId?, programId?, idempotencyKey: /^tp1-[0-9a-f]{16}$/, payload, dependsOn: string[], summary }`, `ContentFlag { kind: 'prompt_injection'|'conflicting_deadline'|'unsupported_claim'|'other', source, excerpt }`, `TraceEvent`, `RunReport`, `ArtifactResult { actionId, app, idempotencyKey, status, ref?: {id, url?}, attempts, detail? }`, `FailureClass` (7, enum order: skipped_work, out_of_scope_work, instruction_violation, integration_failure, retry_loop, hallucination, communication_failure).
Phase 4 trace vocabulary (04-01 `agent/types.ts` TRACE + 04-02):
- `action.execute` span: the START event has attrs actionId, app, tool, key. The END event has status ok|deduped|skipped|error, `durationMs`, and attrs actionId, app, tool, key, attempt, maxAttempts, outcome, reason?, detail?, refId?, 'error.kind'?, 'error.status'?, retryAfterMs?, foundByKey?
- `retry` events: attrs actionId, attempt, 'error.kind', 'error.status', retryAfterMs, foundByKey
- `verify.readback` span END: attrs actionId, app, tool, key, found: boolean, artifactStatus
- Other events have no row: `action.skipped` (reason not_approved), `plan.built`, `policy.*`, `tool.call`, `idempotency.*`, `guard.*`, `run.end`
UI-SPEC data contracts (verbatim targets for evals.ts):
```ts
EvalsFile = { generatedAt: string; commitSha: string; n: number;
  models: { id: string; label: string; kind: 'scripted' | 'llm' }[];
  scenarios: { id: string; name: string; description: string;
    results: Record<modelId, { runs: number; passed: number; passRate: number; passK: boolean | number; failures: Partial<Record<FailureClass, number>> }> }[];
  totals: Record<modelId, { runs: number; passed: number; passRate: number; passK: number }>;
  silentFailure?: { file: string; scenarioId: string } }
SilentFailureRun = { recordedAt, commitSha, scenarioId, description, events: TraceEvent[], report: RunReport,
  oracle: { caught: boolean; failureClass: FailureClass | null; expected: string; found: string; stepSpanId: string } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure adapters: trace-rows, pacer, ReplaySource, evals/silent-failure contracts + fixtures and tests</name>
  <files>web/src/lib/ui/trace-rows.ts, web/src/lib/ui/pacer.ts, web/src/lib/ui/sprint-source.ts, web/src/lib/ui/evals.ts, web/src/lib/ui/fixtures.ts, web/src/lib/ui/trace-rows.test.ts, web/src/lib/ui/sprint-source.test.ts, web/src/lib/ui/evals.test.ts</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: "Trace step (display status)", "Mode detection" (SprintSource + replay semantics), "Static recording files", "Step 4: Run trace", "Screen 2: Eval dashboard" (data contract, sections 1–4)
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-01-SUMMARY.md, web/src/lib/ui/status.ts, web/src/lib/ui/data.ts
    - .planning/phases/04-sprint-pipeline-on-mocks/04-03-SUMMARY.md (trace vocabulary as built) and the first ~80 lines of web/static/data/hero-run.json
    - .planning/phases/06-*/06-0*-SUMMARY.md (evals.json + silent-failure artifact paths and shapes as built), web/static/data/evals.json, web/static/data/silent-failure-run.json (or whatever path Phase 6 used)
    - web/src/lib/core/schemas.ts (TraceEvent, RunReport, Plan, FailureClass)
  </read_first>
  <behavior>
    fixtures.ts exports builders that produce zod-valid data: `ev(partial)`, `sampleHeroRun()` and `sampleEvals()`.
    - `sampleHeroRun()`: plan with 4 actions:
      - `n1` notion write
      - `c1` calendar write
      - `g1` gmail write, payload.to ['advisor@example.edu']
      - `r1` github read
      Plus preflight { n1: 'new', c1: 'exists', g1: 'new' }.
      Events:
      - `plan.built`
      - `action.execute` start/end n1: ok, attempt 1
      - `retry` event c1: attempt 1, error.kind rate_limit, error.status 429, retryAfterMs 50, foundByKey false
      - `action.execute` c1: deduped, attempt 2
      - `action.execute` g1: ok, attempt 3
      - two `retry` events for g1
      - `verify.readback` spans n1 found true, c1 found true, g1 found false with artifactStatus 'mismatch'
      - `run.end`
      Report: artifacts n1 verified, c1 deduped, g1 mismatch; counts {1,1,1,0}; status partial.
      rerun: 3 deduped action spans + readbacks, report all deduped, status ok.
    - `sampleEvals()`: 2 models, fake (scripted) and qwen (llm); 3 scenarios.
      - happy: fake 10/10 passK true; qwen 3/3
      - injection-doc: fake 9/10 passK false, failures { instruction_violation: 1 }; qwen missing
      - lying-api: fake 10/10, failures {}
      totals included; silentFailure { file: 'data/silent-failure-run.json', scenarioId: 'lying-api' }.
    - `sampleSilentFailureRun()`: 2 calendar action spans (ok) plus 2 readbacks. The second readback has found false and spanId 'sR2'. oracle { caught: true, failureClass: null, expected: 'Event created (id cal-…)', found: 'No event with key tp1-… on read-back', stepSpanId: 'sR2' }.
    trace-rows.test.ts:
    - toTraceRows(sample.events, sample.plan):
      - returns 6 rows in start order with index 1..6
      - row n1 kind 'action', status 'ok', summary from the plan, durationMs is a number
      - row c1 status 'deduped' with 1 attempt line
      - row g1 status 'retried', attempt 3, attemptLines.length 2, and a line containing '429' and 'waited 50 ms'
      - readback rows have kind 'readback' and tool label 'readback'; g1's readback status is 'failed' with labelOverride 'Not found on read-back'
    - Other statuses:
      - END status 'skipped' with attrs.reason 'policy' → status 'blocked'
      - reason 'other' → the row is omitted
      - status 'error' → 'failed'
      - a START with no END → 'running'
    - isRowTerminal: true only for the END of `action.execute` / `verify.readback`. countRowSpans(sample.events) === 6.
    - rowAnnouncement(row) === 'Step 3, Gmail gmail.createDraft: Retried ×3' (uses APP_LABELS + traceChip label; tool from attrs). runAnnouncement(report) === 'Run finished: 1 verified, 1 deduped, 1 failed.'
    - oracleStepIndex(rows, 'sR2') === the 1-based index of that row; an unknown span id → rows.length.
    sprint-source.test.ts:
    - filterRecordedRun(file, all write ids) → events unchanged; report counts equal the recorded ones.
    - filterRecordedRun(file, ['n1','c1']):
      - no event with attrs.actionId 'g1' remains; events without actionId are kept
      - the g1 artifact status is 'skipped' with detail 'not_approved'
      - counts { verified 1, deduped 1, failed 0, skipped 1 }; approvedIds ['n1','c1']; status 'ok'
    - Status rule: 'failed' when verified+deduped === 0 and failed > 0; 'partial' when failed > 0; else 'ok'.
    - createReplaySource(file, { pace }) with a `pace` spy:
      - kind 'replay'
      - buildPlan() resolves { plan, preflight }
      - execute(plan, ['n1','c1','g1'], onEvent): onEvent receives the events in order; pace is called exactly countRowSpans(events) times, each AFTER a row-terminal event; resolves to the filtered report
      - rerun is defined when file.rerun exists and undefined otherwise
      - estimateSteps(['n1']) === countRowSpans(filtered events)
    - playEvents with an AbortSignal aborted mid-way rejects with ReplayCancelled and emits no further events.
    - pacer (injected `sleep` spy that resolves immediately):
      - playing → pace() resolves and sleep was called with 400
      - setIntervalMs(100) → next sleep called with 100
      - pause() → pace() is still pending after 3 microtask flushes; step() resolves exactly one pending pace
      - play() resolves all waiters
      - cancel() rejects pending paces with ReplayCancelled; reset() allows pacing again
      - onChange listeners fire on play/pause
    evals.test.ts:
    - parseEvalsFile(sampleEvals()) ok. parseEvalsFile({}) → ok false with a message. parseEvalsFile of `web/static/data/evals.json` (read via node:fs) → ok true, with scenarios.length >= 8.
    - primaryModel → the first `kind 'scripted'` model (fallback: first model); llmModel → the first 'llm' model or null.
    - taxonomy(evals, 'fake'):
      - returns exactly 7 entries in FailureClass enum order
      - instruction_violation count 1; zero counts included
      - each entry has label + definition matching the UI-SPEC table; max === 1
    - topFailure(scenario injection-doc) === 'instruction_violation'; topFailure(happy) === null. Ties resolve by enum order.
    - scenarioAllPassed({ passK: true }) true; ({ passK: 1 }) true; ({ passK: 0.5 }) false.
    - adversarialCount(sampleEvals()) === null when no scenario has `adversarial`/`tags`; with `tags: ['adversarial']` on 2 scenarios → 2.
    - parseSilentFailureRun(sampleSilentFailureRun()) ok. If `web/static/data/silent-failure-run.json` (or the Phase 6 path) exists, it parses ok (`it.runIf(existsSync(path))`).
  </behavior>
  <action>
RED first: write the fixtures and the three test files from <behavior>, run them, and confirm they fail. Then GREEN:
1. `trace-rows.ts`:
   - Type:
     ```ts
     interface TraceRow {
       spanId: string; index: number; kind: 'action' | 'readback'; actionId: string; app: AppName | string; tool: string; summary?: string;
       attempt?: number; maxAttempts?: number; durationMs?: number; status: DisplayStatus; labelOverride?: string;
       attemptLines: string[]; found?: boolean; artifactStatus?: string
     }
     ```
   - `toTraceRows(events, plan?)`: one pass.
     - Collect START/END by spanId for names 'action.execute' and 'verify.readback', ordered by first appearance.
     - Group `retry` events by attrs.actionId.
     - Map statuses per UI-SPEC "Trace step (display status)" and the 04-01 mapping comment. The blocked reasons are ['not_approved','policy','allowlist','injection']; any other skipped reason → omit the row.
     - Readback rows: found === false → 'failed' + labelOverride 'Not found on read-back'; otherwise 'ok'.
     - Attempt line format: `try ${attempt} · ${status} ${kindLabel} · waited ${retryAfterMs} ms` + (foundByKey !== undefined ? ` · re-checked key: ${foundByKey ? 'found' : 'not found'}` : ''). kindLabel: rate_limit → 'rate limited', server → 'server error', timeout → 'timeout', else the raw kind.
     - Read attrs defensively with small helpers (`num(v)`, `str(v)`); never throw on unknown attrs.
   - Also export `isRowTerminal(e)`, `countRowSpans(events)`, `rowAnnouncement(row)`, `runAnnouncement(report)` and `oracleStepIndex(rows, spanId)`.
2. `pacer.ts`:
   - `export class ReplayCancelled extends Error { constructor() { super('replay cancelled'); this.name = 'ReplayCancelled'; } }`
   - `createPacer({ intervalMs = 400, sleep = (ms) => new Promise(r => setTimeout(r, ms)), playing = true } = {})` returns `{ pace, play, pause, step, cancel, reset, setIntervalMs, get playing, get intervalMs, onChange }`. Keep a waiter queue of `{resolve, reject}`.
   - pace():
     - if cancelled → reject
     - if playing → await sleep(intervalMs); then if cancelled → reject; if paused → enqueue a waiter
     - if paused → enqueue
   - step() shifts one waiter; play() flushes all; cancel() rejects all and sets cancelled; reset() clears cancelled.
3. `sprint-source.ts`:
   - Type:
     ```ts
     interface SprintSource {
       kind: 'live' | 'replay';
       buildPlan(profile?: Profile): Promise<{ plan: Plan; preflight?: Record<string, 'new' | 'exists'> }>;
       execute(plan: Plan, approvedIds: string[], onEvent: (e: TraceEvent) => void, signal?: AbortSignal): Promise<RunReport>;
       rerun?(): Promise<{ events: TraceEvent[]; report: RunReport }>;
       estimateSteps?(approvedIds: string[]): number
     }
     ```
     Add a doc comment that Phase 9 adds LiveSource (`/api/plan`, `/api/execute` SSE) implementing the same interface.
   - `filterRecordedRun(file, approvedIds)`: drop events whose `attrs.actionId` is set and not approved. Artifacts for unapproved actions → `{ ...a, status: 'skipped', detail: 'not_approved' }`. Recompute counts (mismatch counts as failed, matching 04-02) and status per the rule; approvedIds = the given ids.
   - `playEvents(events, onEvent, pace, signal?)`: for each event, throw ReplayCancelled if aborted, call onEvent, and `await pace()` when `isRowTerminal(e)`.
   - `createReplaySource(file: HeroRunFile, { pace })`: implements the interface over `filterRecordedRun` + `playEvents`. `rerun` is present only when `file.rerun` exists and resolves it as recorded; the page paces it through `playEvents`.
4. `evals.ts`:
   - Zod `EvalsFile` and `SilentFailureRun` exactly per the UI-SPEC contract. Use `.passthrough()` on scenario objects so optional `adversarial`/`tags` survive.
   - `parseEvalsFile(raw)` / `parseSilentFailureRun(raw)` return the data.ts `Parser` result shape. **This is the single adapter point.** If Phase 6's real file uses different names (e.g. `pass_rate`, `passHatK`, `taxonomy`), normalize here before `safeParse`, and add a test case that uses the real file.
   - `FAILURE_LABELS: Record<FailureClass, { label; definition }>` is copied from the UI-SPEC taxonomy table.
   - Also export `primaryModel`, `llmModel`, `taxonomy(evals, modelId) → { entries: { cls, label, definition, count }[]; max }`, `topFailure(scenario)`, `scenarioAllPassed(result)`, `adversarialCount(evals)` and `silentFailurePath(evals) = evals.silentFailure?.file ?? DATA_PATHS.silentFailure`.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test -- src/lib/ui/trace-rows.test.ts src/lib/ui/sprint-source.test.ts src/lib/ui/evals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with >= 25 passing tests across the 3 files
    - `grep -cE "export (function|async function|class|const|interface) (toTraceRows|isRowTerminal|countRowSpans|rowAnnouncement|runAnnouncement|oracleStepIndex)" web/src/lib/ui/trace-rows.ts` == 6
    - `grep -cE "export (function|interface|class) (createReplaySource|filterRecordedRun|playEvents|SprintSource)" web/src/lib/ui/sprint-source.ts` == 4
    - `grep -c "export class ReplayCancelled" web/src/lib/ui/pacer.ts` == 1
    - `grep -cE "export (const|function) (EvalsFile|SilentFailureRun|parseEvalsFile|parseSilentFailureRun|FAILURE_LABELS|taxonomy|topFailure)" web/src/lib/ui/evals.ts` >= 7
    - `grep -c "static/data/evals.json" web/src/lib/ui/evals.test.ts` >= 1 (the real Phase 6 file is contract-tested)
    - `grep -rnE "from '\\$app|from 'svelte" web/src/lib/ui/trace-rows.ts web/src/lib/ui/pacer.ts web/src/lib/ui/sprint-source.ts web/src/lib/ui/evals.ts` prints nothing
  </acceptance_criteria>
  <done>The recorded sprint and eval artifacts map through tested adapters into view models (rows, filtered reports, taxonomy, silent-failure oracle step), with pacing controls and cancellation. Phase 6/9 shape changes touch only these modules.</done>
</task>

<task type="auto">
  <name>Task 2: Sprint steps 3–5: PlanApproval + ActionCard, TraceTimeline + ReplayControls, ArtifactsPanel, wired into +page.svelte via ReplaySource</name>
  <files>web/src/lib/components/PlanApproval.svelte, web/src/lib/components/ActionCard.svelte, web/src/lib/components/TraceTimeline.svelte, web/src/lib/components/ReplayControls.svelte, web/src/lib/components/ArtifactsPanel.svelte, web/src/routes/+page.svelte</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: "Step 3: Plan & approve", "ActionCard.svelte", "Sticky CTA bar", "Step 4: Run trace", "Step 5: Verified results", "Layout grid" (trace grid columns), "Copywriting Contract", "Accessibility Contract" items 2–4, 9, 10, "Screen 3"
    - web/src/routes/+page.svelte (07-01 version: sections, state, step nav)
    - web/src/lib/ui/trace-rows.ts, pacer.ts, sprint-source.ts (Task 1); web/src/lib/ui/mode.svelte.ts, status.ts, format.ts, config.ts (07-01)
    - web/src/lib/components/StatusChip.svelte, EmptyState.svelte (07-01)
  </read_first>
  <action>
1. `ActionCard.svelte`: props `{ action, checked, preflight, disabled, ontoggle, describedById }`.
   - Outer `<div class="rounded-lg border bg-surface p-4 space-y-2" class:border-accent={checked} class:border-border={!checked}>` containing a `<label class="min-h-11 flex gap-2 items-start">` with a native checkbox (`size-5 text-accent`, `aria-describedby`).
   - Line 1: the summary plus a preflight badge ('new' → idle "New"; 'exists' → info "Exists: will dedupe"; hidden when there is no entry).
   - Line 2 (`text-sm text-fg-muted`, wraps, `id={describedById}`): app label · `font-mono` tool · target `{schoolName — programName}` (resolved from DATASET by schoolId/programId when set) · key `font-mono` · "after {dependsOn.join(', ')}" when non-empty.
   - For gmail: "Draft to {payload.to joined} · never sent".
   - The `<details>` "Preview payload" `<pre class="font-mono text-sm overflow-x-auto max-h-64 rounded-md bg-bg border border-border p-2">` sits OUTSIDE the `<label>`, so opening it never toggles the checkbox.
2. `PlanApproval.svelte`: props `{ plan, preflight, approved: SvelteSet<string>, running, mode, pristine, onRun }`. Per UI-SPEC "Step 3":
   - Replay + `!pristine` → info note "The recorded plan was built for the demo profile. Your edits change the gap report above, not this plan."
   - Blockers: fail callouts (`TONE_CLASSES.fail`) with the message and the code in mono.
   - Flags: block callouts. Title "Ignored instruction found in {source}" for prompt_injection, the kind label otherwise; `<blockquote class="font-mono text-sm">{truncate(excerpt,160)}</blockquote>`; then "Treated as data. It cannot add or change actions."
   - Toolbar: master checkbox "Approve all ({n})" with `bind:indeterminate`; "Clear selection" text button; `<span aria-live="polite">{selected} of {total} selected</span>`.
   - Write actions are grouped in PLAN_APP_ORDER: `<fieldset class="space-y-2" disabled={running}>` whose legend is a tri-state group checkbox "{App label} ({count})".
   - Reads: "Reads (no approval needed): {tools}" (muted).
   - Sticky CTA bar (`sticky bottom-0 bg-surface border-t border-border py-4 flex flex-wrap items-center justify-between gap-4`):
     - left: "Replaying the recorded run" (replay) / "Drafts only · mock apps" (live)
     - right: the primary button `bg-accent text-accent-fg hover:bg-accent-hover rounded-md px-4 min-h-11 font-semibold`, labelled "Run {n} approved actions" or "Running…"
     - disabled when n === 0 or running, with helper `<p id="cta-help">Select at least one action to run.</p>` wired via `aria-describedby` when n === 0
3. `ReplayControls.svelte`: props `{ playing, index, total, onPlay, onPause, onStep, onPrev?, onRestart, speedMs?, onSpeed? }`. All buttons are secondary (`bg-bg border border-border text-fg hover:bg-surface rounded-md px-4 min-h-11`).
   - Pause/Play toggle with `aria-pressed={playing}`.
   - When `onPrev` is given (manual mode): "Previous step", then **"Next step"** as the accent-filled button, then "Step {index} of {total}" with `aria-live="polite"`.
   - Otherwise: "Step".
   - "Restart".
   - A Speed `<select>` from REPLAY_SPEEDS when `onSpeed` is given (compressible).
4. `TraceTimeline.svelte`: props `{ rows, total, currentIndex = -1, announce = true, finalMessage = '' }`.
   - Progress: `<label for="trace-progress" class="text-sm">{done} of {total} steps</label><progress id="trace-progress" class="w-full h-2 accent-accent" max={total} value={done}>`, where done = rows with a terminal status.
   - Column header (`hidden sm:grid grid-cols-[3rem_8rem_1fr_5rem_5rem_8rem] gap-2 text-sm font-semibold text-fg-muted`): Step · App · Operation · Attempt · Latency · Status.
   - `<ol aria-label="Run trace steps" class="divide-y divide-border">`. Each `<li>` uses the same grid at sm+ and the two-line layout below 640px. It shows:
     - `#{index}` (mono, tabular-nums)
     - the app label
     - `font-mono` tool (readback rows show `readback`) with the summary truncated (`truncate`, full text in `title`)
     - "try {attempt}/{maxAttempts}"
     - "{fmtMs} ms"
     - `<StatusChip chip={row.labelOverride ? { ...traceChip(row.status, row.attempt), label: row.labelOverride } : traceChip(row.status, row.attempt)} />`
   - Current row (`i === currentIndex`): `border-l-2 border-accent bg-surface`.
   - Rows with `attemptLines.length` get `<ul class="pl-12 text-sm text-fg-muted">`.
   - Live region `<p class="sr-only" aria-live="polite" aria-atomic="true">{message}</p>`. A `$effect` sets `message = rowAnnouncement(lastTerminalRow)` when the terminal count grows (only if `announce`), and `finalMessage` when it is non-empty. Only `motion-safe:transition-colors` is allowed.
5. `ArtifactsPanel.svelte`: props `{ report, plan, onRerun?, rerunning }`. Per UI-SPEC "Step 5":
   - Run status banner `role="status"` using runStatusChip copy.
   - Count chips (verified ok, deduped info, failed fail, not approved idle) via `countTone`.
   - Artifacts grouped in PLAN_APP_ORDER. Each `<li class="rounded-md border border-border p-2 flex flex-wrap items-center gap-2">`: artifactChip + the action summary from the plan + `ref.id` mono + "Open ↗" when `ref.url` + "{attempts} attempt(s)", with the detail on a second line (muted).
   - Report blockers/flags repeat as in Step 3.
   - "Run again" secondary button with helper "Same plan, same keys. Every action should come back deduped." when `onRerun` is set.
6. `web/src/routes/+page.svelte`: extend the 07-01 page and keep steps 1–2 untouched.
   - State:
     - `plan = $state<Plan | null>(null)`, `preflight = $state<Record<string,'new'|'exists'> | undefined>()`, `approved = new SvelteSet<string>()`
     - `events = $state<TraceEvent[]>([])`, `rows = $derived(plan ? toTraceRows(events, plan) : [])`
     - `report = $state<RunReport | null>(null)`, `running = $state(false)`, `total = $state(0)`, `finalMessage = $state('')`, `runError = $state('')`
     - `pacer = createPacer()`, `playing = $state(true)` (synced through `pacer.onChange`), `speedMs = $state(400)`
     - `source = $derived(appState.hero?.state === 'ok' ? createReplaySource(appState.hero.data, { pace: pacer.pace }) : null)`
   - Build plan:
     - disabled while `appState.mode === 'detecting'`, `!valid`, or no source (helper text states why)
     - `const r = await source.buildPlan(profile)`; set plan/preflight; `approved.clear()` and add every write action id (default all selected)
     - `await tick()` and focus `#plan-heading` (h2 with `tabindex="-1"`)
   - If `appState.hero?.state === 'error'`: EmptyState tone fail, heading "Recorded run not available", body `data/hero-run.json could not be loaded (${status ?? reason}). The gap report still works; the plan and trace need a recording. Run npm run record and rebuild.`
   - Run:
     - `running = true; events = []; report = null; finalMessage = ''; total = source.estimateSteps?.([...approved]) ?? 0`
     - `pacer.reset()`; focus `#trace-heading`
     - `report = await source.execute(plan, [...approved], (e) => events.push(e), controller.signal)`
     - `finalMessage = runAnnouncement(report)`
     - Catch: ReplayCancelled is ignored (restart); any other error → `runError = "The run stopped at step {rows.length}: {message}. Completed steps are shown below; nothing past step {rows.length} was written."`
     - `finally running = false`
   - Restart: abort the controller, `pacer.cancel()`, then run again.
   - Run again: `const r = await source.rerun()`; events reset; `playEvents(r.events, e => events.push(e), pacer.pace)`; then `report = r.report`.
   - Replay controls render above the TraceTimeline only in replay mode and while rows exist or running. Speed → `pacer.setIntervalMs(ms)`.
   - Sections #plan/#trace/#results: show the EmptyStates when there is no data (the UI-SPEC copy stays as in 07-01), otherwise PlanApproval / TraceTimeline / ArtifactsPanel. Step nav ✓ for 3/4/5 when plan / rows.length / report exist.
   - `onDestroy` → `pacer.cancel()`.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test && npm --prefix web run check && MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build && grep -q "3. Plan &amp; approve\|3. Plan & approve" web/build/index.html && grep -q "4. Run trace" web/build/index.html && grep -q "5. Verified results" web/build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web test` and `npm --prefix web run check` exit 0; the static build exits 0
    - `grep -c "createReplaySource(" web/src/routes/+page.svelte` == 1 and `grep -c "source.execute(" web/src/routes/+page.svelte` == 1 and `grep -c "rerun" web/src/routes/+page.svelte` >= 1
    - `grep -c "Approve all" web/src/lib/components/PlanApproval.svelte` == 1, `grep -c "indeterminate" web/src/lib/components/PlanApproval.svelte` >= 2, `grep -c "Select at least one action to run." web/src/lib/components/PlanApproval.svelte` == 1, `grep -c "Ignored instruction found in" web/src/lib/components/PlanApproval.svelte` == 1
    - `grep -c "Exists: will dedupe" web/src/lib/components/ActionCard.svelte` == 1 and `grep -c "never sent" web/src/lib/components/ActionCard.svelte` == 1 and `grep -c "Preview payload" web/src/lib/components/ActionCard.svelte` == 1
    - `grep -c "aria-live=\"polite\"" web/src/lib/components/TraceTimeline.svelte` >= 1 and `grep -c "aria-atomic" web/src/lib/components/TraceTimeline.svelte` == 1 and `grep -c "assertive" web/src/lib/components/*.svelte` totals 0
    - `grep -c "Verified means the item was read back from the app after the run" web/src/lib/components/ArtifactsPanel.svelte web/src/routes/+page.svelte` totals >= 1
    - `grep -c "Run npm run record and rebuild" web/src/routes/+page.svelte` == 1
  </acceptance_criteria>
  <done>From the static site a judge can build the recorded plan, approve all or some actions, watch a paced, controllable, screen-reader-announced trace with text status chips and retry sub-rows, finish on the verified-artifacts panel, and replay the idempotent rerun.</done>
</task>

<task type="auto">
  <name>Task 3: Eval dashboard: StatCard, ScenarioTable, TaxonomyBreakdown, SilentFailureReplay, evals/+page.svelte</name>
  <files>web/src/lib/components/StatCard.svelte, web/src/lib/components/ScenarioTable.svelte, web/src/lib/components/TaxonomyBreakdown.svelte, web/src/lib/components/SilentFailureReplay.svelte, web/src/routes/evals/+page.svelte</files>
  <read_first>
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md: "Screen 2: Eval dashboard" (header, sections 1–4, States), "Eval rates", "Layout grid" (Eval dashboard row), "Copywriting Contract" (Evals empty/error, Silent-failure file missing), "Accessibility Contract" items 5, 8
    - web/src/routes/evals/+page.svelte (07-01 shell)
    - web/src/lib/ui/evals.ts, trace-rows.ts (Task 1); web/src/lib/ui/data.ts, status.ts, format.ts, config.ts (07-01)
    - web/src/lib/components/TraceTimeline.svelte, ReplayControls.svelte (Task 2), StatusChip.svelte, EmptyState.svelte
  </read_first>
  <action>
1. `StatCard.svelte`: props `{ label, value, sub, sub2?, chip? }`. Markup `<div class="rounded-lg border border-border bg-surface p-4 space-y-1">`: label (`text-sm text-fg-muted`), then `<p class="text-2xl font-semibold tabular-nums text-fg">{value}</p>` with an optional StatusChip beside it (rate tone; the number itself stays `text-fg`), then sub and sub2 (`text-sm text-fg-muted`).
2. `ScenarioTable.svelte`: prop `{ evals }`, inside `<div class="overflow-x-auto"><table class="w-full min-w-[640px] text-sm">` with `<caption class="sr-only">Pass rate per scenario and model</caption>`.
   - Headers are `<th scope="col">`: Scenario · Runs · per model "Pass rate · {label}" and "pass^k · {label}" · Top failure.
   - Rows follow file order. The first cell is `<th scope="row">` with the name plus the id in `font-mono text-fg-muted`.
   - Runs = the primary model's runs, or the max across models.
   - Rate cell: `fmtPct(passRate)` text plus an `aria-hidden` `h-2` bar whose width is `style="width: {pct}%"`, coloured by rateTone. Use literal classes via a `{#if}` per tone or a `BAR_CLASSES` literal map: `bg-ok-fg`, `bg-warn-fg`, `bg-fail-fg`.
   - pass^k cell: ok chip "✓ All passed" or fail chip "✕ Not all", from `scenarioAllPassed`.
   - A model with no result shows "Not run" (muted).
   - Top failure: an idle chip with `FAILURE_LABELS[cls].label`, or "—".
3. `TaxonomyBreakdown.svelte`: props `{ evals }`. `modelId = $state(primaryModel(evals).id)`, with a `<select>` ("Model") only when `evals.models.length > 1`.
   - h2 "Failure taxonomy".
   - `<ul>` of `taxonomy(evals, modelId).entries`, all 7 in enum order. Each row: label (`text-sm font-semibold`), count (`tabular-nums`; muted when 0), an `aria-hidden` `h-2 bg-fail-fg` bar with width `count/max*100%` (no bar at 0), and the definition (`text-sm text-fg-muted`).
4. `SilentFailureReplay.svelte`: prop `{ run }`.
   - `rows = $derived(toTraceRows(run.events))`; `i = $state(0)`; `stepIdx = $derived(oracleStepIndex(rows, run.oracle.stepSpanId))`; `playing = $state(false)`.
   - A `$effect` runs a `setInterval` of 400 ms while playing (`i = Math.min(i + 1, rows.length)`, and stop at the end); return the clearInterval.
   - Wrapper `<section role="group" aria-label="Silent failure replay" tabindex="0" onkeydown={ArrowLeft → prev, ArrowRight → next}>`.
   - h2 "Caught silent failure" plus the body `{run.description}`, falling back to the UI-SPEC copy.
   - `<ReplayControls playing index={i} total={rows.length} onPrev onStep={next} onPlay onPause onRestart />` (manual mode).
   - `<TraceTimeline rows={rows.slice(0, i)} total={rows.length} currentIndex={i - 1} announce={false} />`.
   - When `i >= stepIdx`, show `<div class="grid sm:grid-cols-2 gap-4">`:
     - "What the tool returned": ok chip "OK" + `oracle.expected`
     - "What read-back found": fail chip `{ tone:'fail', glyph:'≠', label:'Not found' }` + `oracle.found`
   - When `i === rows.length`, a verdict callout:
     - caught → ok tokens: `Caught. The artifact was marked mismatch and the run reported ${run.report.status}, not ok.`
     - otherwise → fail tokens: `Missed. Classified as ${FAILURE_LABELS[failureClass]?.label ?? 'unclassified'}.`
5. `web/src/routes/evals/+page.svelte` (replace the 07-01 shell body; keep the title, H1, subtitle and BRIEF link):
   - `evalsState = $state<LoadResult<EvalsFile> | null>(null)`, `sfState = $state<LoadResult<SilentFailureRun> | null>(null)`.
   - `onMount`: `evalsState = await loadStatic(`${base}/${DATA_PATHS.evals}`, parseEvalsFile)`; on ok, `sfState = await loadStatic(`${base}/${silentFailurePath(evalsState.data)}`, parseSilentFailureRun)`.
   - Meta line: "Commit `{sha7}` · Generated {fmtDateTime(generatedAt)} · {n} runs per scenario".
   - Loading: four `motion-safe:animate-pulse h-24 rounded-lg bg-surface` blocks, an `h-64` block, and visible `role="status"` "Loading eval results…".
   - Error: EmptyState tone fail "Couldn't load eval results" + `data/evals.json returned {status ?? 'an invalid format'}. The numbers are also in BRIEF.md.` + a "Read BRIEF.md ↗" link.
   - Empty (`scenarios.length === 0`): "No eval results yet" / "Run npx tsx scripts/eval.ts --n 10 in web/, then rebuild the site."
   - Ok:
     - Stat cards `grid grid-cols-2 lg:grid-cols-4 gap-4`: Pass rate (the primary model's `totals.passRate`, chip by rateTone, sub "{passed} of {runs} runs · {label}", sub2 "{llm label}: {rate}" when `llmModel`), pass^k (`totals.passK`, sub "Scenarios where all {n} runs passed"), Scenarios (count; sub "Including {k} adversarial" or "Seeded, adversarial suite"), and Silent failures caught ("1" when `sfState` is ok and `oracle.caught`, else "0"; sub "Lying-API run caught by read-back").
     - ScenarioTable.
     - `grid lg:grid-cols-12 gap-6`: TaxonomyBreakdown in `lg:col-span-4`; `lg:col-span-8` holds SilentFailureReplay when sfState is ok, otherwise EmptyState "Replay not available" / "The recorded silent-failure trace is missing from this build."
   - Page wrapper `pt-8 space-y-12`. The BRIEF link is visible in every state.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test && npm --prefix web run check && MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build && grep -q "Eval results" web/build/evals/index.html && grep -q "BRIEF.md" web/build/evals/index.html && test -f web/build/data/evals.json && test -f web/build/data/hero-run.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web test` and `npm --prefix web run check` exit 0; the static build exits 0 and copies `data/evals.json` and `data/hero-run.json` into `web/build/data/`
    - `grep -c "parseEvalsFile" web/src/routes/evals/+page.svelte` >= 1 and `grep -c "parseSilentFailureRun" web/src/routes/evals/+page.svelte` >= 1
    - `grep -c "scope=\"row\"" web/src/lib/components/ScenarioTable.svelte` == 1 and `grep -c "<caption" web/src/lib/components/ScenarioTable.svelte` == 1 and `grep -c "Not run" web/src/lib/components/ScenarioTable.svelte` == 1
    - `grep -c "taxonomy(" web/src/lib/components/TaxonomyBreakdown.svelte` >= 1 and `grep -c "aria-hidden" web/src/lib/components/TaxonomyBreakdown.svelte` >= 1
    - `grep -c "What the tool returned" web/src/lib/components/SilentFailureReplay.svelte` == 1, `grep -c "What read-back found" web/src/lib/components/SilentFailureReplay.svelte` == 1, `grep -c "ArrowRight" web/src/lib/components/SilentFailureReplay.svelte` == 1, `grep -c "oracleStepIndex(" web/src/lib/components/SilentFailureReplay.svelte` == 1
    - `grep -c "Couldn't load eval results" web/src/routes/evals/+page.svelte` == 1 and `grep -c "Replay not available" web/src/routes/evals/+page.svelte` == 1 and `grep -c "Loading eval results" web/src/routes/evals/+page.svelte` >= 1
    - `grep -rnE "bg-\\$\\{|text-\\$\\{|border-\\$\\{" web/src/lib/components` prints nothing (no dynamic Tailwind class names)
  </acceptance_criteria>
  <done>The dashboard renders the committed eval results: headline pass rate and pass^k, per-scenario table per model, the full 7-class taxonomy, and a keyboard-steppable replay of the caught lying-API failure with the claim vs read-back comparison and verdict. Loading, empty and error states are honest.</done>
</task>

</tasks>

<verification>
Run sequentially from Git Bash in the repo root:
1. `npm --prefix web test`: every suite is green (core + `lib/ui` foundation, trace-rows, sprint-source, evals).
2. `npm --prefix web run check`: 0 errors.
3. `MSYS_NO_PATHCONV=1 BASE_PATH=/multi-app-agent npm --prefix web run build`: exits 0. `web/build/index.html` contains all 5 step headings; `web/build/evals/index.html` contains "Eval results"; `web/build/data/{hero-run,evals}.json` exist.
4. `git diff --name-only -- web/src/lib/core web/src/routes/api web/package.json` prints nothing.
5. Manual check (optional, during 07-03's checkpoint): `npm --prefix web run dev`, then open `/?replay`. Build plan → Run shows paced rows, results panel and Run again. `/evals/` shows cards, table, taxonomy and the silent-failure step-through.
</verification>

<success_criteria>
- ROADMAP Phase 7 criterion 2: human-readable action cards (app, operation, target, payload preview, new vs exists), approve all or individual actions, a step timeline with status chips ending in a verified-artifacts panel.
- ROADMAP Phase 7 criterion 3: the eval dashboard shows per-scenario pass rate over N, pass^k, the taxonomy breakdown, and a replay of the caught silent-failure trace.
- UI-01 and UI-02 are satisfied on static data. A Phase 9 LiveSource drops in behind `SprintSource` with no component changes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-02-SUMMARY.md`. Include:
- the exported signatures of trace-rows, pacer, sprint-source and evals (Phase 9 LiveSource implements `SprintSource`)
- any Phase 4/6 shape adaptations made in the adapters
- whether the silent-failure artifact was present
- component props
</output>
