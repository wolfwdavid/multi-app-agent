---
phase: 07-sprint-ui-eval-dashboard-static-showcase
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/smoke-static.mjs
  - scripts/deploy-hf.sh
  - space/README.md
  - .github/workflows/deploy-pages.yml
autonomous: false
requirements: [UI-01, UI-02, DATA-03]

must_haves:
  truths:
    - "One zero-dependency command builds the static site for a given base path and proves it works the way GitHub Pages serves it: `/`, `/evals/` and every relative or root-absolute href/src they reference return 200 under the base; the static JSON files (hero-run, evals, and the silent-failure file when referenced) parse; and `.nojekyll` exists"
    - "Building for the wrong base path makes the smoke check fail (base-path regressions are caught)"
    - "`scripts/deploy-hf.sh` builds with BASE_PATH='', smoke-checks the result, stages README.md with `sdk: static` frontmatter, and uploads `web/build` to the WolfDavid/multi-app-agent Space using the local HF token. It is never run in CI, and `--dry-run` does everything except upload"
    - "The Pages workflow keeps BASE_PATH=/<repo> and adds `npm run check` plus the static smoke step before uploading the artifact"
    - "After the user approves publishing, https://wolfwdavid.github.io/multi-app-agent/ and /evals/ plus the HF static Space serve the new UI with the recorded run and real eval JSON (remote smoke passes)"
  artifacts:
    - path: "scripts/smoke-static.mjs"
      provides: "static build + base-path-faithful local server + link/JSON smoke; --serve for manual review; --url for live sites"
      min_lines: 120
    - path: "scripts/deploy-hf.sh"
      provides: "manual HF static Space deploy (hf upload web/build)"
      contains: "hf upload"
    - path: "space/README.md"
      provides: "HF Space card with sdk: static"
      contains: "sdk: static"
    - path: ".github/workflows/deploy-pages.yml"
      provides: "Pages build with check + smoke"
      contains: "smoke-static.mjs"
  key_links:
    - from: ".github/workflows/deploy-pages.yml"
      to: "scripts/smoke-static.mjs"
      via: "node ../scripts/smoke-static.mjs --base <repo name> after npm run build"
      pattern: "smoke-static\\.mjs --base"
    - from: "scripts/deploy-hf.sh"
      to: "scripts/smoke-static.mjs"
      via: "node scripts/smoke-static.mjs --build --base '' before upload"
      pattern: "smoke-static\\.mjs --build"
    - from: "scripts/deploy-hf.sh"
      to: "space/README.md"
      via: "cp space/README.md web/build/README.md before hf upload"
      pattern: "space/README\\.md"
---

<objective>
Ship the static showcase:
1. A custom, zero-dependency static smoke tool that serves `web/build` exactly like a sub-path static host, crawls every internal reference, and validates the static JSON.
2. A manual HF static Space deploy script.
3. Pages workflow hardening (type check + smoke before deploy).
4. After user approval, publish to GitHub Pages and the HF Space and smoke-test both live URLs.

Purpose: ROADMAP Phase 7 success criterion 4, the "Public Pages + HF showcase live with real eval numbers" checkpoint. Publishing is gated on the user because it pushes every unpushed commit to the public repo and overwrites the public Space.
Output: 2 scripts, the Space card, workflow edits, and live deployments.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md
@.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-02-SUMMARY.md
@tasks/lessons.md

<conventions>
- OWNERSHIP: only the files in `files_modified`. Do not edit `web/**`. If the smoke finds a UI bug, fix it in the owning 07-01/07-02 file, and record it as a deviation in the SUMMARY.
- The HF token is local only. `scripts/deploy-hf.sh` must never be referenced from `.github/workflows/**`. Never print or echo tokens.
- Git Bash converts `/multi-app-agent` in env vars AND CLI args into `C:/Program Files/Git/multi-app-agent` (verified). Therefore:
  - `smoke-static.mjs` accepts `--base multi-app-agent` (no leading slash) and normalizes it to `/multi-app-agent`.
  - `--build` spawns the Vite build itself with `BASE_PATH` set inside Node, so nothing path-like crosses the MSYS boundary.
- Known build facts:
  - adapter-static with `fallback: '404.html'` and `trailingSlash = 'always'` emits `index.html`, `evals/index.html`, `404.html`, `.nojekyll`, `_app/**`, and copies `static/data/*.json` to `build/data/`.
  - Prerendered HTML uses RELATIVE asset and link paths (`./_app/...`, `./evals/`). `404.html` uses root-absolute `/<base>/_app/...`.
- HF static Space URL: take `subdomain` from `https://huggingface.co/api/spaces/WolfDavid/multi-app-agent` and use `https://<subdomain>.static.hf.space`, falling back to `https://wolfdavid-multi-app-agent.static.hf.space`. The Space currently contains a Gradio `app.py` + `requirements.txt`, which the deploy removes.
- `hf upload` flags (verified locally): `hf upload REPO_ID [LOCAL_PATH] [PATH_IN_REPO] --repo-type space --delete PATTERN... --commit-message MSG`.
- COMMITS and PUSH:
  - Commit messages must NOT contain `Co-Authored-By`, `Claude`, `Claude-Session`, `Generated with`, or any AI-assistant mention. The HF `--commit-message` also must not.
  - Before any `git push`, run `bash scripts/strip-ai-trailers.sh origin/main`, then the independent check `git log origin/main..HEAD --format=%B | grep -inE "co-authored|claude|anthropic"`, which must print nothing (tasks/lessons.md).
  - Only rewrite unpushed commits, and only when no other executor is mid-commit.
</conventions>

<interfaces>
Existing `.github/workflows/deploy-pages.yml` build job steps (working-directory `web`): checkout → setup-node 24 (npm cache, web/package-lock.json) → `npm ci` → `npm test` → `npm run build` with `BASE_PATH: /${{ github.event.repository.name }}` → `actions/upload-pages-artifact@v3` (path web/build). Deploy job: `actions/deploy-pages@v4`.
Existing `space/README.md` frontmatter: title Multi-App Agent, emoji 🔗, colorFrom indigo, colorTo blue, sdk static, app_file index.html, pinned false, license mit, short_description.
`web/vite.config.ts`: adapter-vercel when `VERCEL` is set, else adapter-static; `paths.base = process.env.BASE_PATH ?? ''`.
Page markers (07-01/07-02): `/` prerender contains "Application sprint" and "TransferPilot"; `/evals/` contains "Eval results". Static JSON: `data/hero-run.json` (`plan.actions[]`, `events[]`, `report.status`), `data/evals.json` (Phase 6; the `silentFailure.file` pointer is optional), `data/silent-failure-run.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/smoke-static.mjs + scripts/deploy-hf.sh + space/README.md + Pages workflow check/smoke steps</name>
  <files>scripts/smoke-static.mjs, scripts/deploy-hf.sh, space/README.md, .github/workflows/deploy-pages.yml</files>
  <read_first>
    - .github/workflows/deploy-pages.yml, space/README.md, scripts/strip-ai-trailers.sh (script style: set -euo pipefail, repo-root cd)
    - web/vite.config.ts (BASE_PATH/VERCEL switch)
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-UI-SPEC.md sections "Non-negotiable platform rules", "Screen 3: Static showcase behavior", "Build Priority" (static deploy smoke row)
    - .planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-02-SUMMARY.md (actual data file paths, silent-failure presence)
  </read_first>
  <action>
1. `scripts/smoke-static.mjs`: ESM, Node >= 22 built-ins only (`node:http`, `node:fs`, `node:path`, `node:url`, `node:child_process`). The header comment gives usage:
   `node scripts/smoke-static.mjs [--build] [--base multi-app-agent|''] [--dir web/build] [--serve] [--port 4174] [--url https://host/base] [--wait 300]`
   - Paths: `ROOT = fileURLToPath(new URL('..', import.meta.url))`; `WEB = join(ROOT, 'web')`; dir defaults to `join(WEB, 'build')`.
   - `normBase(s)`: trim; strip a leading MSYS prefix if present (`/^[A-Za-z]:\/.*?\/Git(\/.*)$/` → `$1`); strip leading and trailing '/'; return '' when empty, else '/' + s.
   - `--build`: `spawnSync('npm', ['run', 'build'], { cwd: WEB, stdio: 'inherit', shell: process.platform === 'win32', env: { ...process.env, BASE_PATH: base, VERCEL: '' } })`. Delete `env.VERCEL` rather than setting it empty, because vite.config uses `!!process.env.VERCEL`. Exit 1 on a non-zero status.
   - Local server (when `--url` is absent), using `http.createServer`, which mimics GitHub Pages:
     - pathname outside `base` → 404
     - strip base; `/` or a path ending in '/' → `index.html` in that dir
     - an existing file → 200 with content-type by extension (.html text/html; charset=utf-8, .js text/javascript, .css text/css, .json application/json, .svg image/svg+xml, .txt text/plain, .png, .ico, .webmanifest, .jsonl application/x-ndjson)
     - an extensionless existing dir → 301 to the path + '/'
     - otherwise → 404 with the body of `404.html` when present
     - path traversal (`..`) → 400
     - Listen on `--port` (default 0 = ephemeral) at 127.0.0.1.
   - `--serve`: print `serving ${dir} at http://127.0.0.1:${port}${base}/` and keep running.
   - Checks: `origin = --url ?? `http://127.0.0.1:${port}``, `prefix = --url ? '' : base`; with `--url`, the base comes from the URL path. All fetches use global `fetch` with `redirect: 'manual'` for 301 detection.
     1. `GET ${prefix}/` → 200, HTML contains "Application sprint" and "TransferPilot".
     2. `GET ${prefix}/evals/` → 200, contains "Eval results".
     3. `GET ${prefix}/data/hero-run.json` → 200, JSON with `Array.isArray(plan.actions) && plan.actions.length > 0 && Array.isArray(events) && typeof report?.status === 'string'`.
     4. `GET ${prefix}/data/evals.json` → 200, valid JSON object (non-empty; `scenarios` array length >= 1 when that key exists).
     5. Silent failure: `sf = evals.silentFailure?.file ?? 'data/silent-failure-run.json'`. When evals references it, OR (local mode) the file exists in dir → it must return 200 and parse with `events` array + `oracle` object. Otherwise print `WARN silent-failure replay file not present`.
     6. Local mode only: `existsSync(join(dir, '.nojekyll'))`.
     7. Link crawl over `/`, `/evals/` and (local mode) `/404.html`:
        - extract `(?:href|src)="([^"#][^"]*)"`
        - skip `http(s):`, `mailto:`, `data:`, `//`
        - `new URL(ref, pageUrl)`; must be same-origin and start with `${prefix}/` (otherwise FAIL with the page + ref: "outside base path")
        - GET each unique URL → 200 (follow at most one 301)
        - also extract `import("./_app/...")` / `"./_app/...js"` string refs in the inline boot script via `/["'](\.{1,2}\/_app\/[^"']+)["']/g`, and resolve + check them the same way
        - Cap at 150 URLs.
     8. `GET ${prefix}/definitely-missing-page/` → 404.
   - Print `PASS <check>` / `FAIL <check>: <reason>` lines and a final `SMOKE OK (<n> checks, <m> urls)` or `SMOKE FAILED (<k> failures)`, then exit 0/1 and close the server.
   - `--wait N` (remote mode): retry the whole check set every 10 s until it passes or N seconds elapse, printing the attempt number. Use a `setTimeout` promise inside Node, not shell sleep.
2. `scripts/deploy-hf.sh` (`#!/usr/bin/env bash`, `set -euo pipefail`). The header says: manual only, uses the local HF CLI login, never run from CI.
   - `SPACE="${HF_SPACE:-WolfDavid/multi-app-agent}"`; `DRY_RUN=0`; parse `--dry-run`; `cd "$(dirname "$0")/.."`.
   - `command -v hf >/dev/null || { echo "error: hf CLI not found (pip install -U huggingface_hub)" >&2; exit 1; }`.
   - `[ -d web/node_modules ] || npm --prefix web ci`.
   - `node scripts/smoke-static.mjs --build --base ''` (HF serves at the root).
   - `cp space/README.md web/build/README.md && grep -q '^sdk: static$' web/build/README.md`.
   - Dry run: print `dry run: web/build staged for $SPACE ($(find web/build -type f | wc -l) files); skipping upload` and exit 0.
   - `hf auth whoami >/dev/null 2>&1 || { echo "error: not logged in to Hugging Face; run: hf auth login" >&2; exit 1; }`.
   - `sha=$(git rev-parse --short HEAD 2>/dev/null || echo local)`.
   - `hf upload "$SPACE" web/build . --repo-type space --delete "app.py" "requirements.txt" --commit-message "Deploy static showcase ($sha)"`.
   - Print the Space URLs (https://huggingface.co/spaces/$SPACE and the static host from the HF API `subdomain`, via `curl -s ... | node -e` parsing, with the fallback) and the note: `web/build now has base ''; rebuild with: node scripts/smoke-static.mjs --build --base multi-app-agent`.
   - `chmod +x` both scripts (`git update-index --chmod=+x` when committing).
3. `space/README.md`: keep the frontmatter keys, but set `title: TransferPilot`, `short_description: Transfer application agent with measured evals` (60 characters or fewer), and keep `sdk: static` and `app_file: index.html`. Body:
   - a one-paragraph description (profile → gap report → approved plan → verified artifacts; eval dashboard; recorded mock-mode run, PII redacted)
   - links: GitHub Pages https://wolfwdavid.github.io/multi-app-agent/, source https://github.com/wolfwdavid/multi-app-agent, BRIEF https://github.com/wolfwdavid/multi-app-agent/blob/main/BRIEF.md
   - no AI-assistant mentions
4. `.github/workflows/deploy-pages.yml`: in the build job, keep every existing step and add:
   - `- run: npm run check` after `npm test`
   - `- run: node ../scripts/smoke-static.mjs --base "${{ github.event.repository.name }}"` right after `npm run build` and before `upload-pages-artifact`
   - Do not add any HF step or secret.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && npm --prefix web test && npm --prefix web run check && node scripts/smoke-static.mjs --build --base multi-app-agent && ! node scripts/smoke-static.mjs --base "" && bash scripts/deploy-hf.sh --dry-run && grep -q "^sdk: static$" web/build/README.md && node scripts/smoke-static.mjs --base "" && node scripts/smoke-static.mjs --build --base multi-app-agent</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/smoke-static.mjs --build --base multi-app-agent` exits 0 and prints `SMOKE OK`, with PASS lines for `/`, `/evals/`, `data/hero-run.json`, `data/evals.json`, `.nojekyll` and the link crawl
    - Against that same `/multi-app-agent` build, `node scripts/smoke-static.mjs --base ""` exits NON-zero (404.html root-absolute `/multi-app-agent/_app` refs fail), which proves base-path regressions are caught
    - `bash scripts/deploy-hf.sh --dry-run` exits 0, prints `dry run:` and leaves `web/build/README.md` containing `sdk: static`; after it, `node scripts/smoke-static.mjs --base ""` exits 0 (HF root build is valid)
    - The final `node scripts/smoke-static.mjs --build --base multi-app-agent` exits 0, so `web/build` is left as the Pages build
    - `grep -c "hf upload \"\$SPACE\" web/build . --repo-type space" scripts/deploy-hf.sh` == 1 and `grep -rn "deploy-hf" .github/` prints nothing
    - `grep -c "smoke-static.mjs --base" .github/workflows/deploy-pages.yml` == 1, `grep -c "npm run check" .github/workflows/deploy-pages.yml` == 1, `grep -c "BASE_PATH: /\${{ github.event.repository.name }}" .github/workflows/deploy-pages.yml` == 1
    - `grep -cE "^(sdk: static|app_file: index.html|title: TransferPilot)$" space/README.md` == 3
    - `grep -inE "claude|anthropic|chatgpt|gemini|generated with" scripts/smoke-static.mjs scripts/deploy-hf.sh space/README.md` prints nothing
    - `git diff --name-only -- web/src/lib/core web/src/routes/api web/package.json` prints nothing
  </acceptance_criteria>
  <done>A single command proves the static build works under the Pages base path (and fails when the base is wrong). The HF deploy is scripted, dry-runnable and CI-free, and the Pages workflow now type-checks and smoke-tests before publishing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review the static showcase locally and approve publishing</name>
  <files>none (review only; no file changes)</files>
  <action>Start the local review server in the background with `node scripts/smoke-static.mjs --base multi-app-agent --serve --port 4174` (run_in_background; web/build is already the Pages build from Task 1). Print `git log origin/main..HEAD --oneline` so the user sees exactly which commits publishing would push. Then present the how-to-verify steps below and wait for the resume signal. Do NOT push or upload anything before the user types "publish".</action>
  <verify>
    <automated>curl -sf http://127.0.0.1:4174/multi-app-agent/ | grep -q "Application sprint" && curl -sf http://127.0.0.1:4174/multi-app-agent/evals/ | grep -q "Eval results"</automated>
  </verify>
  <done>The user has reviewed the local showcase and typed "publish" (or listed issues, which are fixed in the owning 07-01/07-02 files before re-running Task 1 verification and this checkpoint).</done>
  <what-built>
    The complete static showcase (07-01 + 07-02), built for `/multi-app-agent` and smoke-tested, plus the Pages workflow and HF deploy script. Before this checkpoint, the executor starts `node scripts/smoke-static.mjs --base multi-app-agent --serve --port 4174` as a background process, and runs `git log origin/main..HEAD --oneline` to list the commits that publishing would push.
  </what-built>
  <how-to-verify>
    1. Open http://127.0.0.1:4174/multi-app-agent/. The header shows "TransferPilot", Sprint/Evals/GitHub links and the "Recorded run" pill; the blue banner shows the recording date and commit.
    2. Profile panel: change GPA to 5. An inline error appears under GPA and the gap report shows "Showing results for the last valid profile." Set it back to 3.3. Switch to "Demo student (quarter units)" and confirm the discard prompt. The Units rows show the converted semester figures.
    3. On any gap card, click "Verify on official page ↗". It opens the school page in a new tab, and "Retrieved Sep 13, 2026" plus a confidence badge sit beside it. The fictional school shows "Fictional school: no official page".
    4. Click "Build plan". Action cards are grouped Notion / Google Calendar / Google Docs / Gmail with New/Exists badges and a payload preview. Untick one Gmail card, then click "Run N approved actions". Rows appear about every 400 ms with status chips, Pause/Step work, and the results panel shows that action as "Not approved". "Run again" shows every item deduped.
    5. Click "Evals" (the URL must be /multi-app-agent/evals/). You see the stat cards, the per-scenario table (pass rate, pass^k), all 7 taxonomy classes, and "Caught silent failure". Press Next step / ArrowRight until "What read-back found" and the "Caught." verdict appear.
    6. Optional: toggle the OS dark mode. Colors switch and text stays legible.
    7. Review the listed unpushed commits. Publishing runs `git push origin main` (triggering the Pages workflow) and `bash scripts/deploy-hf.sh`, which overwrites the public HF Space (removing the old Gradio app.py).
  </how-to-verify>
  <resume-signal>Type "publish" to deploy to GitHub Pages + HF Space, or describe the issues to fix first</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Publish to GitHub Pages + HF static Space and smoke-test both live URLs</name>
  <files>scripts/smoke-static.mjs, scripts/deploy-hf.sh</files>
  <read_first>
    - tasks/lessons.md (trailer-stripping rules before push)
    - scripts/strip-ai-trailers.sh
    - scripts/smoke-static.mjs, scripts/deploy-hf.sh (Task 1)
  </read_first>
  <action>
Run this only after the user typed "publish" at Task 2. First stop the background `--serve` process.
1. Pre-push hygiene:
   - `git status --porcelain` must show no uncommitted changes from this phase; commit them first with a clean message.
   - `bash scripts/strip-ai-trailers.sh origin/main`
   - `git log origin/main..HEAD --format=%B | grep -inE "co-authored|claude|anthropic"` must print nothing; otherwise STOP and report.
2. `git push origin main`. If it is rejected (non-fast-forward), STOP and report; never force-push.
3. Pages:
   - Find the run for this commit with `gh run list --workflow deploy-pages.yml --branch main --limit 5 --json databaseId,headSha,status` and pick the one whose `headSha == $(git rev-parse HEAD)`. If none exists yet, re-list after the next command (gh usually registers the run within seconds).
   - `gh run watch <id> --exit-status`. On failure, `gh run view <id> --log-failed`, fix the root cause in the owning file, commit, strip trailers, push, and watch again.
   - `node scripts/smoke-static.mjs --url https://wolfwdavid.github.io/multi-app-agent --wait 300` must print `SMOKE OK`.
4. HF:
   - `bash scripts/deploy-hf.sh`. If it reports not logged in, this is an auth gate: ask the user to run `hf auth login` and resume.
   - Resolve the static host from `https://huggingface.co/api/spaces/WolfDavid/multi-app-agent` (`subdomain`), then run `node scripts/smoke-static.mjs --url https://<subdomain>.static.hf.space --wait 300`, which must print `SMOKE OK`. HF static Spaces can take 1–3 minutes to rebuild.
5. Restore the local Pages build: `node scripts/smoke-static.mjs --build --base multi-app-agent`.
6. Only change the scripts if a live-host difference requires it (e.g. the HF static host redirects `/evals` differently). Keep local and CI behaviour identical and record the change.
  </action>
  <verify>
    <automated>cd "C:/Users/Mkaru/Documents/Hello_World/hackathons/Multi App AI Agent Hackathon" && test -z "$(git log origin/main..HEAD --oneline)" && node scripts/smoke-static.mjs --url https://wolfwdavid.github.io/multi-app-agent --wait 120 && node scripts/smoke-static.mjs --url "https://$(curl -s https://huggingface.co/api/spaces/WolfDavid/multi-app-agent | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{console.log(JSON.parse(s).subdomain||'wolfdavid-multi-app-agent')}catch{console.log('wolfdavid-multi-app-agent')}})").static.hf.space" --wait 120</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..HEAD --oneline` is empty (pushed), and `git log origin/main -20 --format=%B | grep -inE "co-authored|claude|anthropic"` prints nothing
    - The latest `deploy-pages.yml` run for HEAD concluded `success`, including the new check and smoke steps
    - The remote smoke on https://wolfwdavid.github.io/multi-app-agent prints `SMOKE OK` (the `/`, `/evals/`, `data/hero-run.json` and `data/evals.json` checks pass, and every referenced asset returns 200)
    - The remote smoke on the HF static host prints `SMOKE OK`, and `curl -s https://huggingface.co/api/spaces/WolfDavid/multi-app-agent` shows `"sdk":"static"`
    - `web/build/404.html` references `/multi-app-agent/_app` again (the local build restored to Pages base)
  </acceptance_criteria>
  <done>The public GitHub Pages site and the HF static Space both serve the sprint UI with the recorded run and the eval dashboard with real numbers, verified by the same smoke tool that guards CI.</done>
</task>

</tasks>

<verification>
1. `npm --prefix web test` and `npm --prefix web run check`: green.
2. `node scripts/smoke-static.mjs --build --base multi-app-agent` → `SMOKE OK`; `node scripts/smoke-static.mjs --base ""` against that build → non-zero.
3. `bash scripts/deploy-hf.sh --dry-run` → exits 0 and stages README with `sdk: static`.
4. After approval, `node scripts/smoke-static.mjs --url https://wolfwdavid.github.io/multi-app-agent` and `--url https://<subdomain>.static.hf.space` → both `SMOKE OK`.
5. `grep -rn "deploy-hf\|HF_TOKEN" .github/` prints nothing, so no HF credentials are used in CI.
</verification>

<success_criteria>
- ROADMAP Phase 7 criterion 4: the static build is live at https://wolfwdavid.github.io/multi-app-agent/ (correct base path, `.nojekyll`) and on the HF static Space. It loads `evals.json` and the recorded hero trace labelled "recorded run", and every internal link resolves (crawl-verified).
- Pages keeps `BASE_PATH=/multi-app-agent`; HF is built with `BASE_PATH=''` and deployed manually with the local token.
- The overrun rule is respected: if the showcase is not live by 5:00 PM ET, report it so Phases 10 and 11 are skipped.
</success_criteria>

<output>
After completion, create `.planning/phases/07-sprint-ui-eval-dashboard-static-showcase/07-03-SUMMARY.md`. Include:
- the smoke tool usage and sample output
- both live URLs, with the HF subdomain actually resolved
- the Pages run id and conclusion
- the HF commit
- any live-host quirks found
- the publish timestamp (ET), for the ROADMAP checkpoint
</output>
